# STATUS — 2026-07-16 (deep audit, evidence-based, read-only)

Every claim below traces to a file path, a command run this session, or a live-deploy probe. Where a
report says "done" and the code says otherwise, it is called out. Zero code was written for this audit.

**Repo at audit:** branch `merge/live-integration`, HEAD `cb36464`, **156 commits**, **65 ADRs**
(`git rev-list --count HEAD` → 156; `ls docs/adr/ADR-*.md | wc -l` → 65).
**Test suite:** `pnpm test` → exit 0, **258 vitest tests pass** across 6 TS packages (see §2). Live web:
`ninety-nu.vercel.app` responds 200 on every route probed. Live API: **not reachable** (see §2).

---

## 1 · FRONTEND — 18 routes, the truth

18 `page.tsx` routes exist. All sampled routes return 200 on the live deploy. The three known scars,
verified against HEAD + live:

| Item | Verdict | Evidence |
|---|---|---|
| **Hero Momentum River** | ✅ **FIXED — renders live** | Live `[data-hero-river]` main canvas = **460×300** (intrinsic + CSS), area 138 000. The 300×150/0×0 pair are hidden lightweight-charts axis widgets (harmless). The `autoSize` fix holds. Not blank. |
| **`/match/[id]`** | ⛔ **BROKEN — live** | `app/match/[id]/page.tsx` does `await params;` then returns `<TerminalScreen />` — the id is never destructured or passed. Live: `/match/wc26-bra-kor` and `/match/wc26-eng-sui` **both render Egypt/Australia** (the featured market). Every match link opens the same match. |
| **`/proofs`** | ⛔ **FABRICATED — live** | `lib/proofs.ts` ships 8 fake sigs (`5Kx7bQ…9f2aTx`, `7pR2mE…4kQ1Ln`, …) rendered by `ProofBadge` under "Settled on-chain." `ProofBadge.tsx` is a cosmetic `<a href="solscan.io/tx/{sig}?cluster=devnet">` with no chain read — clicking a fake sig → Solscan "not found." The "anyone can verify it" claim is a dead link on a public repo. |

**Live read path:** `grep` for importers of `lib/api` and `lib/ws` in `apps/web/src` → **0 files each**.
`NEXT_PUBLIC_USE_FIXTURES` → **0 references** (does not exist). The site is **100% fixtures**; nothing
reads a backend. Believed-0 confirmed.

**Route inventory (all render):** `/` · `/board` · `/terminal` · `/match/[id]` · `/moments` ·
`/moments/[id]` · `/bracket` · `/competition` · `/portfolio` · `/leaderboard` · `/history` ·
`/how-it-works` · `/onboarding` · `/settings` · `/proofs` · `/play` · `/profile/[handle]` · `/replay/[id]`.
`/play` (next-goal mini-game, ADR-060/061) renders real content (27.9 KB, "next goal"/"pick" present).

**Component pass (ADR-063, commit `08a04ec` "24 registry pulls"):** shipped. `magicui / 21st / godui /
skiper` are **copied-in source, re-skinned** (not npm deps — `grep` of `apps/web/package.json` finds
none); `@svar-ui/*` **is** an npm dep, used in `features/games/RoundFilter.tsx` (2 files). `PROVENANCE.md`
has 52 registry rows. Board/terminal/landing render richly (verified in prior sessions). **Not verified:**
that each of the 24 pulls renders individually — MEDIUM confidence (see §least-confident).

**Would it embarrass us if a judge clicked it?** `/match/<anything>` (always the same match) and
`/proofs` (fake "verify on Solscan" links) — **yes, both.** Everything else is presentable.

---

## 2 · BACKEND — live vs claimed

- **Anything deployed? NO.** `infra/fly.api.toml` (`app = "omnipitch-api"`, bom, port 4000) and
  `infra/fly/fly.toml` (`app = "omnipitch"`, iad, multi-proc) exist as **config only**. Live probes of
  `omnipitch-api.fly.dev/health`, `/markets`, and `omnipitch.fly.dev/health` → **unreachable**. Nothing is deployed.
- **Supabase? NOT provisioned.** `.mcp.json` has `--project-ref=YOUR_SUPABASE_PROJECT_REF` — a
  placeholder. No real project, no `SUPABASE_URL`.
- **Redis?** No provisioned instance found — `docker-compose` (local) only.
- **`SETTLEMENT_LIVE`** = `false` — `txoracle_cpi.rs:34` `pub const SETTLEMENT_LIVE: bool = false;`.
- **Tests — real numbers (`pnpm test`, exit 0):** api **144**, worker-jobs **68**, worker-ingest **25**,
  schema **10**, bus **6**, chain **5** = **258 vitest**. `packages/txline` test = a real assert script
  (`tsx scripts/snapshot.ts` → "8 wrappers CI VERIFY OK", mock of real payloads). **Fake-greens found:**
  `apps/web` test = `echo 'no tests yet'`; `worker-cortex` test **skipped** (no `.venv` → prints a skip
  message, exits 0). So the claimed **"9 Python pytest" did NOT run** this pass, and the honest total is
  258 TS + the txline verify — not "257."
- **Match schema:** ✅ **now has `minute Int?` and `score String?`** on `model Match`
  (`apps/api/prisma/schema.prisma:34–35`, comment: "was absent — audit gap"). The read path's source
  columns exist. `Market` has no minute/score (they live on `Match`). Schema is ready; the web does not read it (§1).
- **`POST /orders`:** ⛔ **STUB** — `apps/api/src/http/routes/orders.ts` is literally `export {};`
  (with a comment). The engine order path (`src/engine/order.ts`) is implemented and tested, but the REST route is not.

---

## 3 · ON-CHAIN — what's real

- **Program:** `declare_id!("6ps8ao7CVhacnRajvFXWTmkknsRnHfEbWmtQ3nDCdBkj")` (`lib.rs:6`). A **real**
  devnet subscribe tx exists: `2RMQS9tYsfg…GYDPqqtX` (TXLINE-MAP/ADR-015). **Not confirmed this audit:**
  that the program account is *currently* deployed/live on devnet — I did not run an RPC `getAccountInfo`
  (least-confident item).
- **ProofBadge:** **cosmetic.** No `@solana/web3.js` in `apps/web` deps; the component is a pure Solscan
  link. It does **not** read devnet state.
- **Anchor tests:** `tests/omnipitch_core.ts` covers `claim_points` (valid / replay-fails / wrong-proof-
  fails / foreign-leaf-fails = 4) and `post_leaderboard_root` (authority-gated = 1). **`settle_market` has
  ZERO test coverage.** The "5/5 Anchor tests" claim is real but is claim+leaderboard — it does **not**
  cover settlement.

---

## 4 · TXLINE + THIRD-PARTY — USED / PARTIAL / NOT

| Integration | Status | Evidence |
|---|---|---|
| **TxLINE wrappers (8)** | **PARTIAL** | `packages/txline/src/{client,fixtures,odds,scores,proofs,replay,statkeys,network}.ts` all exist; CI VERIFY parses real captured payloads (mock). Data flows in **tests**, not in production (nothing deployed). |
| **TxLINE live devnet** | **PARTIAL (code) / not running** | `worker-ingest/src/liveAuth.ts` has `devnetFreshSubscriber()` (fresh subscribe tx per handshake) + a mainnet Subscriber (message-signing, saved txSig from env). Booting live needs a **funded devnet wallet / saved subscribe session** (human-gated). No live subscription is running (backend not deployed). |
| **Mainnet SL12** | **code landed, not running** | The mainnet message-signing subscriber is in `liveAuth.ts`. NOTE: the prompt cited "ADR-059" for this — **ADR-059 is the GitBook docs site**, not SL12; the SL12 mechanism is ADR-037 / TXLINE-MAP. |
| **flagcdn / flags** | **USED (baked)** | 118 baked PNGs in `apps/web/public/flags/{w160,w80}/`, git-tracked (ADR-055). No runtime CDN. |
| **TheSportsDB** | **USED (baked)** | `public/teams/*/badge.png`, `jersey.png`, `players/*.jpg` (real squad faces) — git-tracked (ADR-062). |
| **API-Football** | **NOT INTEGRATED** | No assets, no wired path. TXLINE-MAP notes lineups via API-Football are blocked on a human key. |
| **magicui / 21st / godui / skiper** | **USED (copied source)** | Re-skinned components, 52 `PROVENANCE.md` rows; not npm deps. |
| **@svar-ui** | **USED (dep)** | `features/games/RoundFilter.tsx`. |
| **shadergradient / three / framer / gsap / lightweight-charts** | **USED (deps)** | In `apps/web/package.json`, rendered on landing/terminal. |

---

## 5 · BLOCKERS

### Human-gated (agent cannot do — exactly what's needed)
1. **Supabase project** — create it, paste `DATABASE_URL` (pooler + direct) into `apps/api/.env`. Nothing runs without a DB.
2. **Redis** — provision (Upstash/Fly Redis), paste `REDIS_URL`.
3. **Fly deploy** — `fly auth login` + `fly deploy` for `omnipitch-api` and `omnipitch` (needs a Fly account; the tomls are ready).
4. **Funded devnet SOL wallet** at `~/.config/solana/id.json` (for the on-chain subscribe → live TxLINE), plus a saved subscribe session.
5. **Mainnet SOL + TxL** — only if going to SL12 real-time.
6. **API-Football key** — only if live lineups are wanted.
7. **The GitHub push decision** for the branch.

### Agent-fixable (ranked by unblock ÷ effort)
1. **Wire `/match/[id]`** — destructure `id`, pass to a market-selecting TerminalScreen. Highest visible-scar removal, ~10 min, works even fixture-backed.
2. **Fix `/proofs` fabricated sigs** — relabel as illustrative, or render only the one real sig; kill the dead "verify on Solscan" link. Public-repo credibility, low effort.
3. **Fix stale numbers** (§6) — README/SUBMISSION → 156 commits / 65 ADRs / 258 tests. Trivial.
4. **`settle_market` Anchor test** — a negative-path test asserting the `SETTLEMENT_LIVE=false` revert. Low-medium effort.
5. **Implement `POST /orders`** — the engine queue exists + is tested; wire the route. Medium.
6. **Wire the live read path** behind `NEXT_PUBLIC_USE_FIXTURES` — schema now has minute/score. **Blocked on #1–#2 (datastore).**

### The single biggest blocker
**No datastore.** Supabase (Postgres) and Redis are not provisioned — the API cannot boot, so nothing
can be deployed, so no page can show live data. Every downstream item (live read path, real `/match`
data, `/orders`, a live TxLINE subscription) waits on **one Postgres URL + one Redis URL + one Fly
deploy**. This is the gate between here and "backend live + one page showing live data," and it is human-gated.

---

## 6 · SECRETS / SUBMISSION HYGIENE

- **`.keys/` and `.env*` gitignored:** ✅ `git ls-files .keys` and `git ls-files '*.env'` → empty (not tracked). `.gitignore` covers `.env*`, `.keys/`, `*.pem`.
- **Secrets in tracked files / history:** ✅ **clean.** `git log --all -S 'gb_api_'` → 0 hits; a full 220-commit all-refs blob scan for `sk-` / `sbp_` / `ghp_` / `github_pat_` / `xox*` / `AIza` / `AKIA` / `BEGIN … PRIVATE KEY` → **zero hits**. (The GitBook PAT used inline in earlier sessions was never written to a file.)
- **Stale numbers (all wrong):**
  - `README.md`: "257 tests," "57 ADRs" → actual **258 vitest / 65 ADRs** (and cortex pytest didn't run).
  - `docs/SUBMISSION.md`: "81 commits, 51 ADRs" → actual **156 / 65**.
  - `docs/SUBMISSION-FIELDS.md`: "126 commits, 58 ADRs" → actual **156 / 65**.
  - All three claim "TxLINE verified live on devnet" + "5/5 Anchor tests" — both need the caveats in §3–§4.
- **Fake sigs in committed data:** ⛔ **yes** — `lib/proofs.ts` (8 fabricated sigs) ships and renders as
  "verifiable on Solscan." On a public repo this is the one hygiene item that actively misleads.

---

## Bottom line

### The 3 things to do first (in order)
1. **Human: provision Supabase + Redis** (paste `DATABASE_URL` + `REDIS_URL`). Unblocks the entire backend.
2. **Agent: fix `/match/[id]`** so each match link opens its own market. Removes the most-clickable judge scar; no backend needed.
3. **Agent: remove the fabricated `/proofs` sigs** (relabel illustrative or show only the real sig). Kills a dead "verify it" link on a public repo.

### The single biggest blocker
**No provisioned Postgres + Redis.** The API can't run → nothing is deployed → no live data anywhere. Human-gated; everything else is downstream of it.

### The one thing I'm least confident about
**Whether the Anchor program is currently deployed and live on devnet at `6ps8ao7…`.** I have the
`declare_id` and a real historical subscribe tx, but I did **not** run an RPC `getAccountInfo` this audit
to confirm the program account is presently on-chain. (Secondary: MEDIUM confidence that all 24
component-pass pulls render — I confirmed board/terminal/landing/play, not each pull.)
