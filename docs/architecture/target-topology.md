# v2 target topology (the 9-service org chart)
Kept from the comparison doc as the graduation map: ws-gateway, pricing, settlement, replay, ai as separate deployables + admin app. A module graduates ONLY when a scale trigger in README fires.
