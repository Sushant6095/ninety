# Submission doc (grows daily — required by hackathon)
## Core idea
## TxLINE endpoints used
- auth (A1–A3): `POST /auth/guest/start` → guest JWT · on-chain `txoracle.subscribe(12, weeks)` (devnet) · `POST /api/token/activate` → apiToken. Every data call sends BOTH headers (`Authorization: Bearer {jwt}` + `X-Api-Token: {apiToken}`). Impl: `packages/txline/src/client.ts` (tx built via `packages/chain` per law).
- fixtures schedule (F1): `GET /api/scores/schedule` → `TxLineClient.fixtures()` ⚠ path pending Day-0 confirmation.
- scores snapshot (S1): `GET /api/scores/snapshot/{fixtureId}?asOf=` → `TxLineClient.scoresSnapshot()`. Sample: `docs/txline-samples/scores-snapshot.json`.
- odds stream (StablePrice): …
- score events stream: …
- validation proofs: …
- historical (replay): …
## Business/technical highlights
## TxLINE API feedback (what we liked / friction)
## Links: deployed app · demo video · program on Solana Explorer
