# GitHub topics

Paste into the repo's About → Topics (max 20; these are 18, ordered by discovery value).

```
prediction-market
solana
anchor
world-cup-2026
football
real-time
lmsr
event-driven
redis-streams
nextjs
typescript
rust
fastify
websocket
monorepo
turborepo
play-money
hackathon
```

Rationale: lead with the domain (`prediction-market`, `solana`, `world-cup-2026`) because that is what judges and Solana devs search; the stack tags (`nextjs`, `rust`, `redis-streams`) catch framework-based discovery; `play-money` signals the compliance posture up front.
