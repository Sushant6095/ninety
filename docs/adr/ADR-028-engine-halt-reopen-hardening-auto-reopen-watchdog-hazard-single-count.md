# ADR 028 engine halt/reopen hardening — auto-reopen on next mark, bounded mark-tether, stale-mark watchdog, hazard single-count, NaN-safe LMSR (apps/api/src/engine/{index,watchdog,amm,order}.ts)

Status: accepted 2026-07-08. Context: ADR-024/025/026 built the market lifecycle machine, the durable runtime, and the order command. The goal→HALT→reopen(3×→1× decay)→FT→RESOLVING behavior was mostly present, but the task flagged real gaps: reopen was event-only (the `shouldReopen()` helper was dead code, never called by the loop), no stale-mark watchdog, no bounded mark-tether, and a same-ts ordering question — and an adversarial quant review then surfaced a hazard double-count + a NaN-into-ledger path. Decision (implemented, gate-green, engine-guardian 6/6, quant-reviewer PASS): (1) AUTO-REOPEN — `shouldReopen()` is now wired into `engineApply`'s mark branch: a HALTED market past its 10s halt window reopens on the NEXT mark (ADR-005 "next mark → REOPEN") via `transition(reopen)`; deterministic (uses `cmd.at`, never a wall clock) so replay reproduces the reopen + `reopenAt` exactly, and `spreadAt()` then decays 3×→1× over 60s. A mark INSIDE the window just re-anchors and stays HALTED (satisfies the "mark arrives during halt → anchor updates, stays halted" edge). (2) BOUNDED MARK-TETHER — `bHint` clamped to `[b0, 3·b0] = [300, 900]` in the mark branch (ADR-022 bound, defensive at the bus trust boundary like the existing SPREAD_FLOOR); a corrupt-timestamp mark (`Date.parse → NaN`) is now DROPPED (mirroring `fromEnvelope`) rather than journaled at event-time 0. The cortex 5s re-anchor cadence stays upstream-owned (ADR-022). (3) STALE-MARK WATCHDOG (`watchdog.ts`) — a HALTED market whose pricing feed goes stale > 10s is alerted ONCE (re-arms on a fresh mark or when it leaves HALTED). It is OPERATIONAL, not journaled state — an injected wall clock exactly like the RedisLease renew loop (ADR-025); `check(now, haltedIds)` is pure in `now` so it unit-tests without timers. Exposed as `engine.checkStaleMarks()` + a `startWatchdog()` unref'd interval; `metrics.staleMarkAlerts`, `onStaleMark` callback. Determinism review confirmed the watchdog's `Date.now` only touches its own Maps + metrics and READS `lifecycle.status` — it never mutates `EngineMarket`/lane state. (4) HAZARD SINGLE-COUNT (quant BLOCK fix — the pricing bug) — `b_hint` published by cortex is already `b0·(1+κ·hazard)` (ADR-022); the prior code ALSO divided by `spread = 1+hazard`, so hazard cancelled and the ADR-022 dynamic-liquidity signal was a no-op. FIXED by removing `EngineMarket.spread` (and the mark cmd's `spread`, seedMarket's `spread`, the mark handler's `spread=1+hazard`) entirely: the fill `b = state.bHint / max(spreadAt(lifecycle, at), SPREAD_FLOOR)` — hazard applied ONCE (inside bHint), the reopen decay ONCE (spreadAt), effective `b ∈ [100, 900]`. This also fixes the prior unclamped-hazard (b→~90000 near-zero-slippage) and single-event double-penalty findings. (5) NaN-SAFE LMSR (quant CRITICAL fix) — `amm.ts` now uses the log-sum-exp trick (subtract `max(q_i/b)` before `exp`, mathematically identical to `b·ln(Σ e^(q_i/b))`) so a small b + large aggregate q can never overflow `exp` to Infinity; AND `order.ts` rejects a non-finite cost (`PRICE_UNAVAILABLE`) before the balance check on BOTH buy and sell — so a NaN/Inf LMSR result can never enter the journaled fill/ledger (the same class as ADR-026's INVALID_OUTCOME guard). SAME-TS (edge): the per-market lane resolves same-event-time commands by bus ARRIVAL order, NOT reducer event-time precedence — replay-determinism is journal-guaranteed regardless; a cross-topic same-ts reorder buffer is a deliberate FOLLOW-UP (it would fight the ≤300ms one-tick halt). The residual is pinned by a test covering BOTH goal-first (order rejects MARKET_HALTED) and order-first (order fills, then halt). Consequences / VERIFY (gate 9/9, 120 api tests): zero fills in the halt window; monotonic reopen cost decay 3×→1×; auto-reopen on the next mark + reopen effect emitted; ≤300ms halt measured (`performance.now`, well under budget); watchdog alerts once + re-arms + never-saw-a-mark case; hazard applied once; NaN-guard. quant-reviewer PASS (prior BLOCK cleared), engine-guardian 6/6 invariants (single-writer, journal-then-ack, deterministic-time-from-events, amm purity, no-http/ws, halted-rejects-orders). (~) No CLAUDE.md law/hook changed — the changes ENFORCE the existing laws; the watchdog's wall clock is operational (not the reducer). Play-money invariant untouched. (−) Deferred follow-ups (recorded, not re-litigated): cross-topic same-ts reorder buffer; ENGINE-EMITTED LIFECYCLE EFFECTS (auto-reopen/halt/reopen) are not yet routed to a bus topic for WS/clients — the emit adapter (ADR-027) routes trading effects + `settled` only, so an engine-internal auto-reopen is invisible downstream until a lifecycle/status topic + WS consumer lands (external `reopen` feed events still flow); emit effect outbox (ADR-025). Cross-refs: ADR-005 (halt/reopen/decaying-spread this hardens), ADR-022 (b_hint = b0(1+κ·hazard), the single hazard source), ADR-024 (the lifecycle reducer + shouldReopen/spreadAt), ADR-025 (the runtime + operational-timer precedent), ADR-026 (the order command whose fill uses this b + the NaN-guard sibling), ADR-027 (the projection consuming the emitted effects).
