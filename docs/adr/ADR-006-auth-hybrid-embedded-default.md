# ADR 006 auth hybrid embedded default

Status: accepted. Default: embedded wallet behind email login (chain invisible). Optional: connect Phantom/Backpack via sign-message. Every user IS a Solana keypair either way — satisfies both readings of 'sign up through Solana'. Confirm organizer intent in Discord.
