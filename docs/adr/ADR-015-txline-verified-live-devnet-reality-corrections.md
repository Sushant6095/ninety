# ADR 015 txline integration verified live on devnet — reality corrections (reality wins)

Status: accepted 2026-07-07. Context: ADR-013 (client) and ADR-014 (wire schemas) were built against the TXLINE-MAP/docs and a shaped mock, so VERIFY was yellow — logic verified, world-contact pending. The env was then unblocked with a funded devnet wallet (`~/.config/solana/id.json`); this session made real world-contact — fetched the on-chain txoracle IDL, ran the full subscribe→activate→snapshot flow live against txline-dev + devnet, and captured real payloads (live subscribe tx `2RMQS9tYsfgnRz42pUih4meEXTB6LeDSgtjfprG51vcAdKxVZJd9G7tEsZz8WzyjC9rjmLHCjQNFw9rZgYDPqqtX`, activate 200, `GET /api/fixtures/snapshot` + `/api/scores/snapshot/18193785` USA v Belgium printed authenticated). Several map/doc assumptions were wrong; per "reality wins" the code, schemas, and samples were corrected to observed reality, and ADR-013/014 details are hereby corrected (both cross-linked). Decision (verified live 2026-07-07): (1) Real IDL saved to `packages/txline/txoracle.json` (program `6pW64gN…`, v1.4.2); `subscribe(service_level_id: u16, weeks: u8)` with 9 accounts, PDAs `token_treasury_v2` + `pricing_matrix`, Token-2022 ATAs, discriminator `[254,28,191,138,156,179,183,53]` — built in `packages/chain` (`buildSubscribeIx` + `deriveSubscribeAccounts` + `buildCreateUserAtaIx`) per the "only chain builds Solana txs" law. (2) The user's Token-2022 ATA for the TxL mint must be created (idempotent) BEFORE subscribe — the program does not create it (`AccountNotInitialized` otherwise). (3) Devnet free tier = service level 1 (60s delay); SL12 is MAINNET real-time and returns `InvalidServiceLevelId` on devnet, so the client `serviceLevel` default is now cluster-aware (devnet 1, mainnet 12) — corrects ADR-013's "level 12". (4) F1 fixtures path is `/api/fixtures/snapshot`; the earlier `/api/scores/schedule` guess was WRONG — corrects ADR-014; O1/O2/O3 (`/api/odds/snapshot|updates|stream`) were correct. (5) `/api/token/activate` body is `{ txSig, walletSignature (base64 ed25519 detached, node crypto == nacl), leagues: number[] }` and returns the apiToken as a BARE STRING (not JSON); `guest/start` returns `{ token }`; client fixed, `leagues` default `[]` (standard bundle) — corrects ADR-013's `{ signature, wallet, cluster }` guess. (6) Wire schemas rewritten to the REAL shapes: top-level arrays, PascalCase, nested `Score{Participant1/2{H1,HT,H2,Total{Goals,YellowCards,Corners}}}`, `Clock`, `Stats` map; odds are per-bookmaker records with parallel `PriceNames`/`Prices`/`Pct`; stat-validation is a real Merkle bundle (`statToProve`, `{sub,main,stat}TreeProof` of `{hash,isRightSibling}`); schemas use `.passthrough()` so wrappers return real types without brittleness. (7) statKeys correction: goals live in `Score.*.Total.Goals`, NOT the numeric `Stats` map — the 1002/1003≈goals guess was wrong; `statkeys.ts` now exposes `goalsFromScore()` and retracts the guess. (8) The mock (`packages/txline/src/mock.ts`) now serves the REAL captured samples from `docs/txline-samples/`, so the CI test (`pnpm --filter @omnipitch/txline test`) validates schemas against reality; the live manual runner is `packages/chain/scripts/txline-live.mjs`. Consequences: (+) prompt 05 is now VERIFY GREEN (world-verified), unblocking 06–08/07 which need real subscribe + stat-validation; (+) schemas/paths/flow match the live API and `docs/txline-samples` are real. (−) samples are trimmed to representative sizes (live scores/odds arrays run 100KB–560KB); stream endpoints returned no events for a scheduled fixture, so a stream event = one `ScoreState`/`OddsTick` (documented, not separately sampled); (−) the numeric `Stats`-map key→metric table is still ⚠ Day-0. No CLAUDE.md law or hook changed — the "only chain builds Solana txs" and "only txline calls TxLINE" laws were upheld; the VERIFY three-states rule was codified in `.claude/prompts/PROGRAM-STANDARD.md`. Corrects/cross-refs ADR-013 (client) and ADR-014 (wire schemas).

Extended-by: ADR-016 (2026-07-07) — session 06-LIVE made live in-play world-contact during WC26 Round-of-16 and closed this ADR's open stream question (its "stream endpoints returned no events" consequence): devnet SL1 stream TRANSPORT latency measured ≈0.7–1.2s median (fresh — this is transport, NOT the by-design 60s data-vs-real-match delay — so no topology STOP), the SSE wrapper now `safeParse`-skips `{Ts}` keepalive frames instead of throwing, a real `OddsTick` stream sample is captured at `docs/txline-samples/odds-stream-event.json` (the scores stream was observed emitting only keepalives, so the `ScoreState` stream shape stays inferred from snapshot/updates), and 06 is now VERIFY GREEN unblocking 07.
