# ADR 068 Landing = notio, literally pulled, then made Ninety

Status: accepted 2026-07-17 (`merge/live-integration`, `2bb6295`). Supersedes the notio posture in ADR-066 + PROVENANCE line 107 ("structure study only, zero notio markup ships"). Refines ADR-067 (the HeroRiver yRange fix, carried forward unchanged). Cross-ref: ADR-052/059 (animation libs), ADR-055 (baked flags), the Design + Verification laws in CLAUDE.md.

Context: a direct user instruction, explicitly overriding the adapt-not-clone / intent-not-pixel defaults: pull notio (styleui.dev) **literally** via `shadcn add https://styleui.dev/r/notio.json` — the real code, its deps kept — then re-skin to Ninety, keep our product pieces (HeroRiver, halt, CTAs) and skiper19/39/52. The prior landing was a hand-built notio-*structured* page (ADR-066); this replaces it with notio's actual components.

Decision — **pull literally, re-skin via a one-place token bridge, drop only placeholder brand + play-money conflicts.**
- **Pull.** `shadcn add` wrote ~80 files. app/page.tsx now composes notio's real `navbar · hero · beam · features · feature-card · stats · counter · footer` + its shadcn ui primitives (button/card/badge/input/label/separator/toggle/dropdown/grid-pattern), on notio's deps (`@base-ui/react`, `motion`, `clsx`+`tailwind-merge`, `tw-animate-css`).
- **Make it Ninety — tokens on the OUTPUT, not per-component.** notio is shadcn-semantic (`bg-background`, `text-primary`, `bg-card`, `border-border` …); our Tailwind v3 palette is Ninety-named (`bg`, `surface`, `up` …) with none of those. So a **single shadcn-semantic bridge** (tokens.css defines `--background:var(--bg)`, `--primary:var(--up)`, `--card:var(--surface)`, `--border:var(--hairline)` … all *references*, no third palette copy; tailwind.config maps the color keys with the color-mix+`<alpha-value>` pattern so opacity modifiers work) re-skins the ENTIRE pull to Ninety colors with zero edits to notio's components. Plus: `darkMode:"class"` + a permanent `html.dark` (so notio's `dark:` variants resolve deterministically, not by OS pref); `font-aleo`→Archivo; `rounded-4xl` + `--radius-*`; notio's orange hero/footer radials + beam orbs re-skinned to a dark **green (hero) / violet (footer)** wash.
- **Ninety identity.** Our `Wordmark` + `WC26` chip in nav + footer, our copy ("Every match is a market for ninety minutes", "The whistle is the opening bell", the play-money footer line), "Open the terminal" as the one filled CTA. The hero's right column is **HeroRiver** (our live Momentum River, yRange 0–100) in place of notio's `<Transcriber/>`. skiper39 `CrowdBand` rides the footer edge; skiper19/52 files retained. `Counter` gained thousands-grouping so 1000 reads "1,000".
- **Dropped (placeholder brand / play-money conflict / external-CDN):** logo-cloud, team, testimonial (pulls `cdn.brandfetch.io` avatars), pricing (a "$/mo" tier collides with the play-money invariant — a hard GATE), transcriber, tool/transcription-feature, the theme-switcher chrome, and notio's `src/public` brand SVGs (Coinbase/Uber/Notion — third-party marks we won't ship). This is sanctioned drop-the-placeholder-brand, not defensive stripping.
- **Reconciled the pull's collisions.** macOS case-insensitivity let `shadcn add` overwrite our app-wide `Avatar.tsx`/`Skeleton.tsx` (lowercase `avatar/skeleton.tsx`) — restored from HEAD (fixed ~12 `size={number}` call sites + `PageSkeleton`). The pull imports `cn` from the `utils` alias (`src/lib/format`), where our `cn` did NOT live (it's in `src/lib/utils`) — re-exported `cn` from format so every pulled primitive resolves without touching them. Added `<main>`/`<header>`/`<nav>` landmarks (the notio page had none).

Rejected: (a) editing every notio component to Ninety class names — the bridge is ~40 lines vs ~80 files; (b) importing notio's v4 `index.css` (`@theme inline`, `@custom-variant`) — our stack is Tailwind v3, so it would not compile; kept our `globals.css` instead; (c) keeping Pricing — a pricing tier is a play-money violation, non-negotiable.

Consequences / VERIFY (LOCAL PRODUCTION, both worktree and the main checkout): `pnpm --filter web build` green (18 pages, `/` 10.2 kB) in BOTH; `tsc` clean; **axe 0** on `/`. Screenshotted `/` at lg+xl and **LOOKED** — reads as notio's design wearing Ninety's skin: dark, green River hero, Ninety wordmark, our copy. Read-out-loud: hero `CAN-MAR 1-0 · LIVE 74' · CAN TO WIN 62.4`; stats **rest on 48 Teams / 104 Fixtures / 1,000 credits** (a direct post-animation DOM read — the 28/61/590 in the still was a mid-count frame); footer play-money line + notio/skiper attribution. Hero River canvas **502px** (visible), not the 300 blank default; the two 300px canvases are `cssW:0` hidden ghosts. Attribution added to README Credits + the landing footer. The old hand-built landing files (LandingPage/LandingNav/FreeCredits/BoothQuotes) remain in the tree, no longer routed. Not pushed — merged locally to `merge/live-integration` per the instruction ("merge when green").
