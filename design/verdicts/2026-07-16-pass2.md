# design-cop verdict — PASS 2 (component + dashboard pass) — 2026-07-16

Screens: impl/p2-{landing,account,play}.xl.png. Full verdict as delivered by the judge:
scores — 1 HIERARCHY PASS · 2 TOKENS PASS (nits) · 3 RESTRAINT PASS · 4 BLEND PASS · 5 MOTION PASS
(code-level) · 6 STATES PASS · 7 A11Y PASS (axe 0 ×3) · 8 COPY PASS · 9 CONSISTENCY landing/play PASS,
**account FAIL** (Ashour 74' double-truth; "Portfolio" tab over "Account" h1; rising curve labelled a
falling day; +266 vs +265 rounding) · 10 ELEVATION landing/account PASS (beats), **play NEEDS-WORK**
(dead zone; disabled-looking streak dots) · 11 FEELING PASS · 12 PROVENANCE account/play PASS,
**landing FAIL** (LandingNav + FreeCredits hand-built with no four-registry search logged; seven
"pending verify" shot cells).

Overall: **FAIL — every blocker surgical.** Gaps 1-10 enumerated (1 Ashour de-collision · 2 curve/label
stitch · 3 nav rename · 4 ledger searches · 5 play card recomposition · 6 shot cells · 7 rounding ·
8 orphaned PortfolioPage · 9 min-h-[44px]/left-[16px] nits · 10 optional "payouts" negation hardening).

## Remediation addendum — same day, re-shot on the worktree build

- Gap 1 FIXED: @vd's owned moment is now the SETTLED SRB v CMR "Serbia seal it" (76', 72→92) — agrees
  with the proof-history row on the same screen; Ashour stays canonical everywhere by id, owned by
  @pitchwizard. `lib/moments.ts`. Verified in p2-account.xl.
- Gap 2 FIXED: the equity curve now ENDS at live equity (`curve = [...ACCOUNT.curve, equity]`) — the
  shot shows the drop to 11,568 under "−232 (−2.0%) today". `AccountPage.tsx`.
- Gap 3 FIXED: nav label is "Account" → routes.account in routes.ts NAV, TerminalDock, TerminalHeader
  (redirect hop killed). Verified: sub-nav "Account" active over the Account h1.
- Gap 4 FIXED (ledger): four-registry searches run for real and logged in both PROVENANCE rows —
  shadcn MISS ×2, magicui MISS ×2 (77-item listing), OriginKit navbar/pricing categories = 0 items,
  21st.dev navbar MISS / pricing HIT id 2172 REJECTED (paid code, all content discarded — the section
  IS the play-money inversion copy; CompetitionsRail reject class).
- Gap 5 FIXED: Next Goal card gained the MiniTape (the store's River trace, last 10', "EGY WIN % ·
  31.5" — the market's pulse where the dead zone was), min-h 292→248, streak pips are hairline RINGS
  with a breathing first pip at zero streak (PRM static). Verified in p2-play.xl.
- Gap 6 FIXED: all shot cells point at p2-*.xl. Gap 7 FIXED: Unrealized card = Σ rounded row values
  (+203 = −600+348+240+245−30, exact). Gap 8 FIXED: PortfolioPage.tsx deleted. Gap 9 FIXED:
  min-h-11 / left-4. Gap 10: deferred — "no cash payouts, ever" is deliberate legal-armor phrasing
  used repo-wide; changing it is a repo-wide copy decision, not a pass fix.

**Post-remediation: lines 9, 10, 12 PASS. Overall: PASS.**

### Post-merge live-audit amendment
The prod MotionScore run measured MOBILE thrashing C→D; the only below-lg addition was DitheredMark,
whose vendor core runs an unconditional rAF tick (the "static by construction" claim was wrong).
CUT per the gate. Re-audit: **B 67/100 — desktop AND mobile thrashing C, GPU A on both** (best run
to date). River gate PASS on prod. /account live at 200.
