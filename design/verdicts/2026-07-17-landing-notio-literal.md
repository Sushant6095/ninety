# Verification verdict — landing = notio (literal pull), re-skinned to Ninety · 2026-07-17

Scope: replaced the hand-built notio-*structured* landing with notio's **actual** code (`shadcn add`), re-skinned to Ninety (ADR-068). Method: `pnpm --filter web build` → `next start` on :3100 (worktree) and a confirming build in the main checkout → `screenshot.mjs / notio-landing` at lg+xl → **LOOKED** + a direct post-animation DOM read. Not judged in dev.

- **Reads as "notio wearing Ninety's skin" — SHIP.** Dark #0B0D10 throughout, the green River owns the hero right, Ninety wordmark + WC26 chip, our copy and CTAs. notio's structure (floating-pill nav, split hero, beam-lit feature cards, centered stat band, radial-dome CTA footer) is intact; every colour traces to a Ninety token via the semantic bridge. No orange, no notio brand, no lorem, no light mode.
- **Read-out-loud — PASS.** Nav: `Ninety · WC26 · How it works / Leaderboard / Proofs · Open the terminal`. Hero: `WORLD CUP 2026 · LIVE` / "Every match is a market for ninety minutes" / `CAN-MAR 1-0 · LIVE 74' · CAN TO WIN 62.4`. Features: "Read the game as a market" → Prices move / Booth explains / Solana proves. Stats: **48 Teams · 104 Fixtures · 1,000 Free credits a match** (direct DOM read after the count-up settles; the 28/61/590 in the still is a mid-animation frame). Footer: "The whistle is the opening bell" / "1,000 credits reset free every match…" / "© 2026 Ninety · Play money — no deposits, no cash payouts, ever." / notio+skiper attribution. Nothing disagrees.
- **Hero River (signature) — PASS.** Renders as a filled green rising area (yRange 0–100). Canvas guard: two visible canvases at **502px** (`cssW 502`), not the 300 blank default; two 300px are `cssW:0` hidden ghosts (benign, as on the terminal).
- **Copy law — PASS.** No bet/stake/odds/wager anywhere; play-money framing explicit in hero, stats, and footer. Numbers are mono (`.num`, tabular).
- **a11y — axe 0** on `/` after adding `<main>`/`<header>`/`<nav>` landmarks (was 18 region/landmark violations pre-fix). **tsc** clean; **prod build** green (18 pages) in worktree AND main checkout.
- **skiper kept** — skiper39 CrowdBand renders at the footer edge; skiper19/52 retained.

Verdict: **SHIP.** Note: verifier LOOK-based verdict (not a fresh design-cop-agent run). One follow-up worth a glance later: the stat count-up runs 2.5s, so a fast scroll shows a mid-count frame before it rests on the real numbers — correct values, just a slow settle. Dropped placeholder-brand sections (logo-cloud/team/testimonial/pricing) are documented in ADR-068 as sanctioned, not defensive.
