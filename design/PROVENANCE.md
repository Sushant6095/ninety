# PROVENANCE — component build ledger (Part 5 gate)

One row per component touched in the rebuild. **design-cop criterion 12 FAILS any non-Ninety component in a
diff that has no row here.** `hand-build` is legal ONLY for the six Ninety-specific pieces (MomentumRiver,
MatchCard, PriceChip, trade ticket, ProofBadge, Booth), and only with the searches that came up empty logged.

Router rows are defined in `.claude/skills/ui-craft/SKILL.md §0`.

| Component | Router row | Tool called | Searched (incl. misses) | Sofascore ref | Re-skinned | Shot |
|---|---|---|---|---|---|---|
| BoothLine (terminal/BoothLine.tsx) | Ninety-specific / hand-build | hand-build | shadcn:n/a · magicui:n/a — bespoke halt choreography | motion.md (feedback-not-entertainment) | tokens ✓ | pending |
| Halt overlay (terminal/BigRiver.tsx flash/sweep/wash/cliff/spread) | Ninety-specific / hand-build | hand-build | shadcn:n/a · magicui:n/a — bespoke halt choreography | motion.md (feedback-not-entertainment) | tokens ✓ | pending |
| Halt sequence controller (terminal/useHaltSequence.ts) | Ninety-specific / hand-build | hand-build | shadcn:n/a · magicui:n/a — bespoke halt choreography | motion.md (feedback-not-entertainment) | tokens ✓ | pending |
| Proofs log (features/proofs/ProofsPage.tsx) | Finished product surface / Ninety-specific — hand-build | hand-build (composes Flag + ProofBadge) | shadcn/magicui/21st n/a — a settlement proof log is a bespoke Ninety surface; on-chain proof = the ProofBadge (violet, the ONE chain surface) | n/a | tokens ✓ (violet ONLY on the ProofBadge) | sweep-proofs.{lg,xl}.png ✓ |
| Lineups pitch (terminal/depth/Lineups.tsx) | Football-product structure → sofascore-research — hand-build | hand-build (native SVG, NO iframe) | shadcn/magicui/21st n/a (football-depth lane → sofascore-research); no token-native pitch primitive exists | docs/sofascore-research/components/score-header.md + tabs.md (lineups depth-in-tabs); tokens-only markings, no grass | tokens ✓ (dots re-tinted NEUTRAL this pass — up/down are price-direction tokens, never team colors; dataviz: identity is labeled/positional) | terminal-tab-lineups.xl.png ✓ |
| Match stats bars (terminal/depth/MatchStats.tsx) | Football-product structure → sofascore-research — hand-build | hand-build (dataviz skill consulted this pass) | shadcn/magicui/21st n/a (football-depth lane → sofascore-research) | docs/sofascore-research/components/data-viz.md (proportional comparison bars) | tokens ✓ (bars re-tinted this pass: leading side bg-hi/60, trailing bg-hi/20 — emphasis mirrors the hi/lo value text; up/down reserved for price direction, verified per-row via computed styles) | terminal-tab-stats.xl.png ✓ |
| H2H / form (terminal/depth/H2H.tsx) | Football-product structure → sofascore-research — hand-build | hand-build | shadcn/magicui/21st n/a (football-depth lane → sofascore-research) | docs/sofascore-research/components/rating-badge.md + match-list-and-row.md (recent-form pills + head-to-head meetings) | tokens ✓ (W/L chips are legitimate status use — labeled letters, never color alone) | terminal-tab-h2h.xl.png ✓ |
| Crowd call vote (terminal/depth/CrowdCall.tsx) | Finished product surface → 21st.dev MCP (surgical) | 21st.dev get_component #8243 ruixen.ui "Review Filter Bars" (row = option \| proportional bar \| count, radix RadioGroup) — structure kept, re-skinned to tokens | 21st search "poll vote percentage bar results": no literal poll component; Review Filter Bars is the same anatomy (HIT). Quota spend 1/2 | docs/sofascore-research/components/vote-and-poll.md (who-wins vote; poll ≠ price, footer line says so) | tokens ✓ (hi-ink bars, up ring on your call, num/tabular) | terminal-tab-h2h.xl.png (re-shot final sweep) |
| Avatar facepile (NOT SHIPPED) | Generic primitive → 21st.dev MCP | 21st.dev get_component #17170 "Astryx Avatar Group" — REJECTED on read: it re-exports Meta's @astryxdesign/core + theme package (a second design system, banned by library law). Quota spend 2/2; nothing merged | 21st search "avatar stack facepile overlapping avatars count" (HIT but unusable); facepile itself skipped — YAGNI, leaderboard already carries avatars | n/a | n/a | n/a |
| Scroll area (components/ui/ScrollArea.tsx → CompetitionsRail) | Generic primitive (scroll-area) → shadcn MCP | shadcn MCP: search "scroll area" → view @shadcn/scroll-area (dep: radix-ui, already installed) → get_add_command (`pnpm dlx shadcn@latest add @shadcn/scroll-area`); ported by hand onto the installed unified radix-ui package instead of re-running the CLI (ladder rung 5: no new dep) | shadcn:HIT first try; components.json initialized this pass (repo had none) | docs/sofascore-research/components/match-list-and-row.md (rail lists scroll; Hyperliquid watchlist pattern) | tokens ✓ (hairline thumb, 6px rail, m.transition hover) | terminal-c1.xl.png ✓ |
| Flag disc (components/ui/Flag.tsx) | Any image → next/image | next/image + locally baked PNGs (public/flags, scripts/bake-flags.mjs) | n/a — image primitive; explicit width/height, priority on >28px crests, zero runtime CDN | n/a | tokens ✓ (hairline disc, ring) | flags-competition.xl / flags-portfolio.xl ✓ |
| Match depth tabs (terminal/MatchTabs.tsx) | Generic primitive (tabs) → radix/shadcn | radix-ui Tabs (installed) re-skinned | radix Tabs is the sanctioned tabs primitive — re-skinned to tokens, not hand-rolled | tabs.md | tokens ✓ | terminal-tab-*.png ✓ |
| Events incident timeline (terminal/depth/EventsTimeline.tsx) | Football-product structure → sofascore-research — hand-build | hand-build | shadcn/magicui/21st n/a (football-depth lane routes to sofascore-research, not a UI registry); market-aware minute feed has no drop-in | incident-timeline.md (center spine, minute markers, glyphs, running score) | tokens ✓ (cards neutral — amber is halt-only) | terminal-tab-events.png ✓ |
| Knockout bracket (bracket/BracketB.tsx) | Football-product structure / Ninety-specific — hand-build | hand-build + GSAP round-reveal | shadcn:MISS (no bracket/tournament item, query "tournament bracket knockout tree") · magicui:MISS (animated-candy catalog — marquee/ticker/bento, no data-viz bracket) · 21st.dev:MISS (only generic file-tree "Tree" views + a decorative "Fractal Bloom Tree" — wrong semantics; a knockout tree is a converging match graph, not a collapsible tree-view) | n/a (bespoke tournament tree; Sofascore has no drop-in) | tokens ✓ | bracket-reveal.{lg,xl} ✓ |
| Landing hero (landing/LandingHero.tsx) | Finished product surface — hand-build | hand-build (GSAP word reveal + HeroRiver live-tape clip-path draw-on, reuses Wordmark) | 21st.dev:MISS (query "dark fintech landing hero section" — generic SaaS gradient-blob heroes, wrong aesthetic, re-skin would discard everything) · OriginKit:MISS (hero category returned 0 components) · shadcn/magicui: no hero surfaces | hyperfoundation (structure/restraint only, zero hex) | tokens ✓ (shader slot cut per LANDING DoD — MotionScore B texture budget) | landing.{lg,xl}.png ✓ |
| Number ticker (components/ui/NumberTicker.tsx) | Animated candy → magicui MCP | magicui get_registry_item "number-ticker" (source pulled) | magicui:HIT first try (search "number ticker counter animated" ranked beams first; direct name fetch) | n/a | tokens ✓ (motion/react→framer-motion, num/tabular-nums, token colors from caller, reduced-motion → instant value) | landing.{lg,xl}.png ✓ |
| Loop stage (landing/LoopStage.tsx) | Ninety-specific — hand-build | hand-build (mount-once-on-view wrapper around the REAL FeaturedPanel + useHaltSequence; FeaturedPanel gained a replayNonce prop so each scroll re-entry replays the full halt choreography) | registries n/a — the halt choreography is the product itself; wrapper is 40 lines of IntersectionObserver | motion.md (feedback-not-entertainment) | tokens ✓ | landing.{lg,xl}.png ✓ |
| Landing CTAs + sections (landing/Ctas.tsx, LandingPage.tsx) | Finished product surface — composition | composition (reuses how-hero CTA idiom, Reveal, Footer, routes) | shadcn:n/a (buttons follow the established /how-it-works CTA classes — consistency beats a new pull) | hyperfoundation (one filled CTA repeated verbatim; numbers band headline-less) | tokens ✓ (violet on-chain only, amber halt-chip only) | landing.{lg,xl}.png ✓ |
| Scroll reveal (components/ui/Reveal.tsx) | UI state motion → framer-motion | framer-motion whileInView (pre-existing, reused across board + landing) | registries n/a — a 20-line whileInView wrapper on the sanctioned lib; pulling one would add a dep for less | motion.md (reveals 300–500ms, ease-out, reduced-motion → plain div) | tokens ✓ (m.slow + m.easeOut) | landing.{lg,xl} / board-moved.xl ✓ |
| Price count (landing/PriceScrub.tsx) | Choreographed sequence → GSAP ScrollTrigger | gsap + ScrollTrigger via lib/gsap.ts (ADR-052) | registries n/a — a scroll-triggered counter is 30 lines on the sanctioned lib | hyperfoundation (one number carries the section) | tokens ✓ (one-shot 500ms count on m.count/ninety at trigger; scrub rejected — progress-0 paints 0.0 unscrolled; SSR/reduced-motion hold 61.4) | landing.{lg,xl} ✓ |
| Hero river tape (landing/HeroRiver.tsx) | Ninety-specific (MomentumRiver surface) — hand-build | hand-build (composes MomentumRiver + LivePrice, reads matchLiveStore SSOT) | registries n/a — the River is one of the six Ninety pieces; no registry has a live probability tape | momentum-river skill (canvas, series data, minute axis) | tokens ✓ (up/halt dots, hairline rule, num prices one decimal) | landing.{lg,xl}.png ✓ |
| Section arrivals (landing/LandingScroll.tsx) | Choreographed sequence → GSAP ScrollTrigger | gsap + ScrollTrigger via lib/gsap.ts (ADR-052) | registries n/a — 40 lines on the sanctioned lib; magicui/21st ship framer-based reveals, wrong lib for scroll choreography (ADR-052 splits scroll to GSAP) | hyperfoundation (sections arrive with intent; motion.md restraint) | tokens ✓ (m.slow, heroStagger, ninety ease; reduced-motion → instant) | landing.{lg,xl}.png ✓ |
| Price cells (terminal/PriceCells.tsx) | Ninety-specific — the PriceChip family (one of the six hand-build-legal pieces) | hand-build | registries n/a — the three tradeable H/D/A outcome cells ARE the trade ticket's core; no registry ships a play-money outcome cell wired to the SSOT mark. This pass: base label `shrink-0` so "HOME/DRAW/AWAY" never truncates, and the held-shares indicator became a corner DOT (the ~66px lg cell can't fit "AWAY"+"60 SH" in any arrangement; the count lives in YOUR POSITION) | motion.md (200ms select feedback, active:scale) | tokens ✓ (up ring on selected, halt tint when frozen, no hex) | terminal-after.{lg,xl}.png ✓ (LOOKED; HOME/DRAW/AWAY labels clean, held-dot no overlap at lg+xl) |
| Trade ticket (terminal/TradePanel.tsx) | Ninety-specific — the trade ticket (one of the six) | hand-build | registries n/a — a play-money LMSR buy/sell ticket (Buy/Sell toggle, size slider, quote → Cost/Avg-px/Max-payout, honest halt-disable) is bespoke; no registry ticket is play-money-safe. This pass: metrics `dl` and the action button unstacked from one `flex` row (the button ate the row at lg and collapsed the metrics to 0px) into full-width metrics above + full-width CTA below | motion.md (CTA hover/active/focus) | tokens ✓ (bg-up/bg-down CTA, no bet/stake/odds copy) | terminal-after.{lg,xl}.png ✓ (LOOKED; COST/AVG PX/MAX PAYOUT 65px each, no overlap) |
| Your position (terminal/YourPosition.tsx) | Ninety-specific — trade-ticket family | hand-build | registries n/a — a play-money position readout (value, P/L vs avg entry, shares held) tied to the SSOT; nothing to pull | n/a (position readout) | tokens ✓ (up/down P/L, `60 SH` chip — the authoritative held-count home) | terminal-after.{lg,xl}.png ✓ |
| Terminal dock fold-gate (terminal/TerminalDock.tsx + TerminalScreen sentinel) | Animated candy (dock) → magicui Dock (already logged row 80); this pass adds a VISIBILITY GATE | hand-build gate over the existing magicui Dock | registries n/a — the gate is behavioural (IntersectionObserver on a top sentinel, rootMargin 90% top, no scroll listener): the dock hides while the live-trade fold is in view and reveals on scroll, so a solid overlay never floats over live prices/River | n/a | tokens ✓ (opacity+pointer-events toggle, motion-reduce:transition-none) | terminal-after.{lg,xl}.png ✓ (dock absent at fold — measured opacity 0 at top, 1 after 1400px scroll) |
| Row sparkline (components/ui/Sparkline.tsx) | Ninety-specific data-viz micro-primitive (MomentumRiver's row-scale sibling) — hand-build | hand-build: ~15-line inline-SVG polyline (values→normalized points→`<polyline>`) | registries n/a by DESIGN LAW, not omission — ADR-045 BANS a chart-lib instance per row (a lightweight-charts River in ~22 board rows = a re-render/perf disaster); a registry sparkline would still be that. It is the River's cheap row-scale counterpart, token-coloured, and belongs to the same hand-built data-viz lane as MomentumRiver. context7 n/a (no external API — raw SVG) | n/a (data-viz, not a Sofascore-chrome match) | tokens ✓ (stroke `var(--up)`/`var(--down)` by direction, no raw hex) | board rows in board-after.{lg,xl}.png ✓; direction verified against each mover (NED–USA pink/down, others green/up) |
| Moment card (components/ui/MomentCard.tsx) | Ninety-specific — a captured-swing collectible card (NOT the landing holo card at row ~93, a DIFFERENT file) | hand-build | SEARCH RUN THIS SESSION: 21st `"collectible moment share card sports highlight"` → hits are generic decorative stat cards (Highlight Card / Spotlight Card / Glowing Card), NONE models a swing-moment (rarity tier + EquityCurve slice + owner + honest mint state). magicui n/a. Hand-build justified — it's Ninety product narrative | match-list lineage (Sofascore density, Ninety skin) | tokens ✓ (rarity via RARITY_STYLE — no `chain` on mintless; owner Avatar; honest `mintless`/`Minted`; focus-visible ring-up this pass) | moments-after.lg.png ✓ (LOOKED) |
| Moment hero (components/ui/MomentHero.tsx) | Ninety-specific — the "Moment of the day" hero variant of MomentCard | hand-build | same search as MomentCard — no registry ships a moment-of-the-day hero with a draw-on swing curve | n/a | tokens ✓ (up-green rarity, `color-mix` accents, framer path-draw honors reduced-motion) | moments-after.lg.png ✓ |
| Moment detail / share card (features/moments/MomentDetail.tsx) | Ninety-specific — the full-bleed share card + ProofBadge honesty | hand-build | registries n/a — a play-money, mintless-honest ("MINTLESS — NOT ON-CHAIN", no fake Solscan) share card is bespoke | n/a | tokens ✓ (no fabricated mint sig; Share/Copy CTAs) | moment-detail.lg.png ✓ (LOOKED) |
| Equity / swing curve (components/ui/EquityCurve.tsx) | Ninety-specific data-viz micro-primitive (Sparkline's sibling) — hand-build | hand-build inline-SVG area curve | registries n/a by ADR-045 (a chart-lib instance per card = perf disaster); same hand-built data-viz lane as MomentumRiver/Sparkline. context7 n/a (raw SVG) | n/a | tokens ✓ (stroke/fill `var(--up)`/`var(--down)` by direction) | moments-after.lg.png ✓ |
| Avatar (components/ui/Avatar.tsx) | Generic primitive → next/image over the avatar CDN (next.config remotePatterns: i.pravatar.cc / dicebear) | thin next/image wrapper | SEARCH RUN: magicui `"avatar"` → only `avatar-circles` (overlapping MULTI-avatar), no single deterministic-per-handle avatar; a 1-image wrapper isn't worth a registry pull | n/a | tokens ✓ (hairline ring, rounded) | moments-after.lg.png ✓ (owner avatars) |
| App shell (components/ui/AppShell.tsx) | Ninety-specific composition — the page frame (TerminalHeader + Footer wrapper) | hand-build composition | registries n/a — it composes already-provenanced Ninety chrome (TerminalHeader, Footer); nothing to pull | n/a | tokens ✓ | moments-after.lg.png ✓ |
| Live price (components/ui/LivePrice.tsx) | Ninety-specific (PriceChip lineage — THE price primitive every chip/cell/tape renders) — hand-build | hand-build (tick-counter-keyed 180ms CSS flash; rewritten this pass so rapid same-direction ticks each pulse) | registries n/a — magicui number-ticker is a count-up (wrong semantics for a live tick-flash); the flash IS the product's signature feedback | motion.md (flash 180ms up/down; reduced motion keeps the tint) | tokens ✓ (flash-up/flash-down keyframes on --up/--down) | landing.{lg,xl} + every terminal/board still ✓ |
| Market screener rail (terminal/CompetitionsRail.tsx) | Finished product surface — 21st.dev first | hand-build rework after logged misses (filter pills + column header + dense label-free price cells) | 21st.dev "market screener watchlist dense table live prices": Financial Markets Table (id 9045) REJECTED — full-width dashboard grid + sparkline/Recharts-adjacent styling, wrong for a 340px rail, pull would burn quota for discarded markup; Stock Market Tracker (id 5917) REJECTED — Recharts (banned second chart lib); crypto/sales dashboards irrelevant · shadcn "data table": data-table-demo/dashboard-01 are TanStack full-width tables — new dep class for 13 rows · magicui: animated-list/number-ticker, wrong semantics. Structure studied from hyperscreener.asxn.xyz, zero markup copied | hyperliquid watchlist density | tokens ✓ (amber halt minute, lead-cell hairline tint) | terminal-now.{lg,xl}.png ✓ |
| Tooltip system (components/ui/Tooltip.tsx + HoverCard.tsx + .pop-content) | Generic primitive → radix/shadcn | radix Tooltip/HoverCard re-skinned; ONE app-level provider (skip-delay), origin-aware 150ms entrance (.pop-content keyframe, delayed-open only — instant-open unanimated per emil) | radix is the sanctioned primitive; entrance is 8 lines of CSS on the radix origin var | n/a | tokens ✓ (var(--duration-fast) var(--ease-out)) | terminal-now.xl.png ✓ |
| Prism grid backdrop (landing/PrismGrid.tsx) | Layout accents → OriginKit MCP | originkit get_component "prism-grid" (nextjs/tailwind/ts) — PULLED, re-skinned | first OriginKit pull that survived: CSS-3D (not WebGL, no §7 quarantine), framer-motion only (no new dep). Re-skin: raw-hex candy palette → up/lo/hairline color-mix tokens; Framer runtime leftovers (RenderTarget/ControlType) stripped; reduced-motion → static lines, no fills; fade uses m.easeOut; delivered gradient edge-fade DELETED (design law: no gradients) | n/a (close-section delight budget — rare surface) | tokens ✓ | landing.{lg,xl}.png ✓ |
| Price void (landing/PriceVoid.tsx) | Layout accents → OriginKit MCP | originkit get_component "blackhole" — PULLED, ported | canvas-2D (no WebGL); re-skin: hex palette → resolveColor(up/lo/hairline), stage transparent, gradient sphere shading → flat token disc (no-gradient law), particles 1000→320, dpr≤1.5; rAF gated by IntersectionObserver (offscreen = no loop) + reduced-motion → one static frame | n/a (price-section ambience — everything collapses into the one number) | tokens ✓ | landing.{lg,xl}.png ✓ |
| World globe (landing/WorldGlobe.tsx + Lazy) — ⚠ SUPERSEDED 2026-07-17 by the Crest wall row (long-landing table): the globe was the coldest, most generic element on the page and illustrated nothing the stats didn't already say. No longer rendered by LandingLong; the files remain only for the unrouted LandingPage.tsx | Layout accents → OriginKit MCP | originkit get_component "globe" — PULLED, ported | Framer CDN url-imports → npm three (installed); d3-geo DROPPED (equirectangular is 3 lines); runtime Natural-Earth fetch → BAKED /world-land.png (ADR-055 class; re-bake scripts/bake-land.mjs); dots-only, ocean=surface token, dots=lo, MetLife marker=up; rAF idle-stops + IO-gated; reduced-motion → static, drag still user-driven; next/dynamic ssr:false (three out of initial chunk) | n/a (numbers band — the Jul 19 tile, literally on Earth) | tokens ✓ | landing.{lg,xl}.png ✓ |
| Play-money sticker (landing/StickerPeel.tsx + Lazy) | Layout accents → OriginKit MCP | originkit get_component "sticker-peel" — PULLED, ported (agent-assisted) | CDN three → npm three; RenderTarget/property-controls stripped; imagedelivery default → baked /sticker-play-money.png (scripts/bake-sticker.mjs); zero hex (readToken/getComputedStyle for color defaults); rAF is interaction-gated by design (tween-only, self-stops — verified, no IO needed); reduced-motion → static unpeeled frame; shadowEnabled=false gates shadow-map GPU; next/dynamic ssr:false | n/a (close-section delight; the disclosure it carries is also plain text in hero + footer) | tokens ✓ | landing.{lg,xl}.png ✓ |

## Richness pass (ADR-062) — baked TheSportsDB stillness imagery (crests / jerseys / atmosphere / player faces)

| Component | Router row | Tool called | Searched (incl. misses) | Sofascore ref | Re-skinned | Shot |
|---|---|---|---|---|---|---|
| TeamCrest (components/ui/TeamCrest.tsx) | Any image → next/image | next/image over baked crests (public/teams/, scripts/bake-teams.mjs) | n/a — image primitive, same lane as Flag; a national crest is an emblem, so object-contain free-standing (not disc-cropped); falls back to <Flag> for any unmatched team | score-header.md (crest beside team name) | tokens ✓ (no ring/disc, explicit w/h, priority on hero crests) | richness-match2.xl.png ✓ |
| Match header crests + atmosphere (terminal/MatchHeader.tsx) | Any image → next/image | next/image (crests at 48px + fill backdrop) | n/a — venue image unobtainable (TheSportsDB free tier: strStadiumThump empty, searchvenues blocked), so the HOME side's strFanart1 is the atmosphere at opacity-10 (standard step, not arbitrary) under a surface-token legibility scrim; venue NAME stays the authoritative text (honest gap) | score-header.md (crest header + venue line) | tokens ✓ (scrim = surface-token gradient — sanctioned legibility exception, no raw hex; aria-hidden decorative img alt="") | richness-match2.xl.png ✓ |
| Lineups faces pitch (terminal/depth/Lineups.tsx, rework) | Football-product structure → sofascore-research — hand-build | hand-build (native SVG, NO iframe) — real TheSportsDB squad faces (next/image) bucketed onto the pitch by position | shadcn/magicui/21st n/a (football-depth lane); API-Football XI+formation BLOCKED (no key) → squad+faces are the stillness, captioned "Confirmed XI & formation pending live data"; falls back to the illustrative dot-pitch for any unmatched side | lineups + score-header.md (pitch with player photos, kit accent) | tokens ✓ (hairline markings, jersey = kit-colour accent, no team colours as tokens) | richness-lineups.xl.png ✓ |
| App-wide crest swap (FeaturedPanel, MatchCard, H2H; terminal CompetitionsRail/OpenPositions/NextGoal/PickPad; board LeftRail/GroupStandings/PowerRankings/TopMovers; pages PortfolioPage/HistoryPage/ProofsPage/MomentDetail/BracketPage/GroupTable) | Any image → next/image (TeamCrest drop-in) | TeamCrest swap for <Flag> at every team appearance — one team = one treatment everywhere | n/a — same image lane; drop-in keeps the Flag fallback for any team outside the baked 48 | match-list-and-row.md / score-header.md | tokens ✓ | richness-{board,terminal,match2}.xl.png ✓ |

## Component pass (MagicUI + Skiper + GodUI + SVAR) — research & install ledger

Pulled via `apps/web/scripts/pull-registry-components.mjs` into `src/components/vendor/{magicui,skiper,godui}/`
(raw sources; every shipped one is re-skinned to tokens in place). New npm deps (apps/web):
`rough-notation` (MIT) · `svg-dotted-map` (MIT — repo thejessewinton/svg-dotted-map) · `canvas-confetti` (ISC)
· `@svar-ui/react-core` + `@svar-ui/react-filter` 2.6.0 (MIT). The vendor `motion`/`next-themes`/`@radix-ui/react-icons`
deps were NOT installed — imports rewritten to framer-motion / always-dark / lucide in the re-skin (two-lib law).
Licenses: magicui MIT · godui.design states MIT · skiper-ui.com declares NO license on site or a findable repo —
distributed via its public shadcn registry (intended embedding); flagged here honestly.

Deliberate SKIPS (hard gates / restraint):
| Item | Why skipped |
|---|---|
| @magicui/animated-theme-toggler | HARD-GATE 2: "no light mode v1" is design law — a theme toggle is either a law breach or dead decoration. Not installed. |
| @magicui/globe | The OriginKit WorldGlobe already ships on the landing (PROVENANCE row above) — a second globe is a restraint violation, not a feature. dotted-map covers the map ask on /how. |
| godui spin-viewer | Needs a 360° frame-sequence asset; none exists and stock frames are non-Ninety content. Pulled? no — excluded from the puller. |
| @magicui/video-text | No brand video asset; stock video = non-Ninety content (pulled to vendor/ but not shipped). |

### Shipped rows — component pass

| Component | Router row | Tool called | Searched (incl. misses) | Sofascore ref | Re-skinned | Shot |
|---|---|---|---|---|---|---|
| Live ticker (home/Ticker.tsx on vendor/magicui/marquee) | Animated candy → magicui | registry pull magicui.design/r/marquee | direct name HIT | match-list liveness at a glance | tokens ✓ (`--marquee-*` vars so the loop can never bind the 200ms motion token; clones aria-hidden+inert; reduced → static overflow row; LivePrice tick-flash untouched) | pass-board.xl ✓ |
| Board bento (home/BentoBoard.tsx on vendor/magicui/bento-grid) | Generic layout → magicui | registry pull magicui.design/r/bento-grid | direct HIT | board hierarchy (movers 6 / standings 4 / rankings tall) | tokens ✓ (icon/CTA/next-themes cruft stripped; FeaturedPanel kept OUT — a bento copy would mount a second River) | pass-board.xl ✓ |
| Credits count-up (ui/CreditPill.tsx) | Animated candy → magicui number-ticker idiom | inlined the ported NumberTicker idiom (prev→next roll, not 0→value) | n/a — port already in repo | n/a | tokens ✓ (m.count/m.easeOut; reduced/SSR instant; added focus-visible + press) | pass-board.xl ✓ |
| Traders facepile (home/TradersWeek.tsx on vendor/magicui/avatar-circles) | Animated candy → magicui | registry pull magicui.design/r/avatar-circles | direct HIT | leaderboard facepile | tokens ✓ (remote imgs DELETED — initials discs, zero fetch; pile aria-hidden inside one 44px leaderboard link) | pass-board.xl ✓ |
| Spotlight info cards (RightRail Moment / LeftRail Settlement / TopMovers tiles on vendor/magicui/magic-card) | Animated candy → magicui | registry pull magicui.design/r/magic-card | direct HIT | n/a | tokens ✓ (orb mode deleted; spotlight = color-mix of hi/hairline, chain mixes ONLY on the two on-chain cards; hover-only; reduced → static). LAW TENSION: spotlight is a radial gradient — user-ordered, token-mixed, flagged | pass-board.xl ✓ |
| Notification bell (home/components/NotificationBell.tsx on vendor/godui/notification-inbox) | Generic primitive → godui pull | registry pull godui.design/r/notification-inbox | direct HIT | n/a | tokens ✓ (godui theme absent — every var mapped; oklch presence hues → neutral color-mix; remote avatar field deleted; height anim → opacity/x+layout; dots: chain only on-chain row, halt only halt row; feed content read-out-loud-consistent with rails/booth). Mounted IN TerminalHeader's alerts slot (overlay hack removed same session) | pass-board.xl ✓ |
| Booth terminal chrome (terminal/BoothTimeline.tsx on vendor/magicui/terminal) | Animated candy → magicui | registry pull magicui.design/r/terminal | direct HIT | booth = system log, not entertainment | tokens ✓ (traffic lights → hairline discs; pre/code → wrapping div; typing 25ms/char, m.fast reveals; sequence OFF — live prepends would break positional indexing; reduced instant) | pass-terminal.xl ✓ |
| Terminal dock (terminal/TerminalDock.tsx on vendor/magicui/dock) | Animated candy → magicui | registry pull magicui.design/r/dock | direct HIT | n/a | tokens ✓ (cva dep dropped; solid surface, NO backdrop-blur on the tick path; MAX_SCALE 1.15 hard clamp; m.spring; replay/trade dispatch window events consumed by MatchColumn; reduced → no magnification) | pass-terminal.xl ✓ |
| Traders-in-market strip (terminal/TradersStrip.tsx) | Animated candy → magicui avatar-circles | composition over the shared vendor pull | n/a | n/a | tokens ✓ (SVG data-URI initials, colors read from live CSS vars; 3,412 count appears exactly once — redundant tile removed) | pass-terminal.xl ✓ |
| Loop scramble headline (landing on vendor/magicui/hyper-text) — ⚠ REMOVED 2026-07-17: a scramble on the "Goal. Halt. Reprice." h2 garbled the words for ~700ms exactly while the section asks you to read the panel landing a real goal beside it. The motion belongs to the market, not the h2. No longer rendered by LandingLong; the vendor file survives only for the unrouted LandingPage.tsx | Animated candy → magicui | registry pull magicui.design/r/hyper-text | direct HIT | n/a | tokens ✓ (dead framer wrappers deleted; forced-uppercase removed — sentence-case law; invisible sizer kills reflow; once-in-view; reduced instant) | pass-landing.xl ✓ |
| Copy highlights (Ctas.tsx + proof h2 on vendor/magicui/highlighter) | Animated candy → magicui | registry pull magicui.design/r/highlighter (dep rough-notation MIT) | direct HIT | n/a | tokens ✓ ("PLAY MONEY" up stroke; "Everything is proven." chain stroke on the ONE violet surface; body ResizeObserver dropped — replayed on every lazy mount; reduced → static border-b) | pass-landing.xl ✓ |
| Velocity band (landing/VelocityBand.tsx on vendor/magicui/scroll-based-velocity) | Animated candy → magicui | registry pull magicui.design/r/scroll-based-velocity | direct HIT | n/a | tokens ✓ (PRM bug fixed — vendor kept crawling under reduced motion; text-lo/70 after axe measured /30 at 1.67:1; aria-hidden decorative) | pass-landing.xl ✓ |
| Hero glow (LandingHero.tsx on vendor/magicui/backlight) | Layout accents → magicui | registry pull magicui.design/r/backlight | direct HIT | n/a | tokens ✓ (REBUILT: vendor feGaussianBlur re-blurred the live tape every repaint — now two static color-mix pools, zero animation). LAW TENSION: a glow is a gradient by essence — token-mixed, flagged | pass-landing.xl ✓ |
| Price path (landing/PricePath.tsx on vendor/skiper/skiper19) | Choreographed scroll → skiper | registry pull skiper-ui.com/registry/skiper19 (license: none declared — public shadcn registry, flagged) | direct HIT | n/a | tokens ✓ (demo page deleted; hairline track + up stroke; framer useScroll — does not touch GSAP triggers; 2xl-only aria-hidden; reduced → fully drawn) | pass-landing.xl ✓ |
| Crowd terrace (landing/CrowdBand.tsx on vendor/skiper/skiper39) | Choreographed canvas → skiper | registry pull skiper-ui.com/registry/skiper39 | direct HIT | football crowd metaphor, close section | tokens ✓ (gsap import → lib/gsap.ts wrapper; openpeeps sprites → abstract silhouettes in lo/up; IO-gated ticker+timelines, dpr≤1.5, count 90; reduced → static scatter) | pass-landing.xl ✓ |
| Pillars hover-expand (landing on vendor/skiper/skiper52) | Animated candy → skiper | registry pull skiper-ui.com/registry/skiper52 | direct HIT | n/a | tokens ✓ (BROKEN swiper/css imports removed — pull didn't compile; expansion on hover AND focus-within; reduced → equal width). LAW TENSION: animates flex-grow (layout) — component essence, one row, far from live prices, flagged | pass-landing.xl ✓ |
| Broadcast ticker (home/Ticker.tsx + lib/broadcastFixtures.ts + lib/broadcastData.ts; api /rich/fixtures) | Rebuild a screen on real data (ui-ux-pro-max) — hand-built strap (not a registry primitive) | hand-build; data = football-data.org `/competitions/2000/matches` via new `GET /rich/fixtures/:competition` (ADR-080, same cost-aware proxy + honest-degradation contract as the other /rich routes). Endpoint VERIFIED 200 on free tier before UI (England 1–2 Argentina SEMI, France v England 3RD PLACE, Spain v Argentina FINAL). Pinned to a REAL football-data snapshot (`data/wc26/broadcast-fixtures.json`) like the rest of the fixture-pinned app; live path = `getFixtures()` (confirmed working locally via /api→:4000, 200) | direct HIT (route existed pattern) | real WC26 fixtures/results; flags = baked local `/flags/w80/*` via `lib/flags` FIFA-code map (ADR-055, no runtime CDN — verified `flagCdn:false`); live minute/score/price stays TxLINE/replay-owned via `useMatchLive` (ADR-051), never football-data | tokens ✓ (no raw hex; team names UI-sans semibold, ONLY numbers `.num`+tabular; segments `text-lo` tracking-caps; LIVE `--up`, HALT `--halt`; 180ms LivePrice tick-flash kept on live chips). Marquee ONLY on overflow (verified: 3 chips centered, 11 chips → marquee), pause-on-hover; reduced-motion → static scrollable (verified). Skeleton + honest empty state, never faked | ticker-{board,terminal}-1440.png + wide-8chip ✓ (LOOKED; read-out-loud coherent — no LIVE-vs-tomorrow contradiction; 0 errors) |
| Watch-the-film player (landing/WatchReel.tsx on vendor/skiper/skiper67 `VideoReel`) | Add a component (`pnpm dlx shadcn add @skiper-ui/skiper67`) → ui-ux-pro-max | registry pull skiper-ui.com/registry/skiper67 (installed `media-chrome ^4.19.2`; raw pull landed at ui/skiper-ui, consolidated into the canonical vendor file, duplicate deleted) | direct HIT | Ninety reel `public/video/ninety-reel.mp4` (Video-495 letterbox-cropped `crop=720:762:0:210`, +faststart, 2.0MB) + poster | tokens ✓ (raw cream-hex stage dropped; `bg-bg`/`surface`/`hairline`/`up`; over-video pill+controls keep white via mix-blend-exclusion — arbitrary frames, not a token surface). Cursor-follow Play pill (framer useSpring) → click expands (clip-path spring) into a fullscreen media-chrome player. **PORTALED to `<body>`** (a data-arrive transform ancestor was confining `position:fixed` to the grid cell). Preview IO-gated (plays in view only, ~2MB never loads otherwise); reduced-motion → poster only + plain-fade popover; Escape/backdrop close, body-scroll lock, focus-visible rings. ⚠ LEGAL: same Video-495 named-player likeness placeholder — swap for owned/licensed/anonymous film before public ship | reel-{rest,open2}.png ✓ (LOOKED; preview + fullscreen play; backdrop covers viewport; 0 errors) |
| Icons hover-expand gallery (landing/IconsGallery.tsx on vendor/skiper/skiper52 `HoverExpandGallery`) | Add a component (`pnpm dlx shadcn add @skiper-ui/skiper52`) → ui-ux-pro-max | registry pull skiper-ui.com/registry/skiper52 (added `@skiper-ui` registry to components.json so the user's exact command resolved; the raw pull landed at ui/skiper-ui and was consolidated into the canonical vendor file, then the duplicate deleted) | direct HIT | 7 owned football-icon stills in `public/icons/` (Bellingham, Zlatan, Haaland ×2, Zidane, Messi, Pelé), downscaled to ≤960px | tokens ✓ (raw cream-hex stage + white/50 caption → surface/hairline/up/text-hi + color-mix `--bg` scrim; hover AND focus-within expand; focus-visible ring; name-free alt + factual nation·number caption; reduced → equal width). LAW TENSION: flex-grow (layout) — component essence, one row, landing-only, flagged. ⚠ LEGAL: third-party broadcast/wallpaper likenesses + club/sponsor/FIFA marks — MECHANISM PLACEHOLDER, swap for owned/licensed/anonymous before public ship | icons-{xl}.{rest,hover}.png ✓ (LOOKED; 7 imgs loaded; read-out-loud clean; 0 errors) |
| Moment holo card (landing/MomentCard.tsx on vendor/godui/holographic-card) | Layout accents → godui | registry pull godui.design/r/holographic-card | direct HIT | n/a | tokens ✓ (4 hex colorways → ONE up/hi color-mix foil; canonical Ashour moment from lib/moments — cross-surface consistent with the terminal booth; tilt 6°, hover:hover+pointer:fine only; touch/reduced static). LAW TENSION: foil = gradient — token-mixed, flagged | pass-landing.xl ✓ |
| Play-money dissolve (landing/PlayMoneyDissolve.tsx on vendor/godui/particle-dissolve) | Layout accents → godui | registry pull godui.design/r/particle-dissolve | direct HIT | n/a | tokens ✓ (run-forever rAF fixed — trigger-gated, releases on settle; dpr≤1.5; ~500 particles; aria-label carries the text; reduced → drawn instantly) | pass-landing.xl ✓ |
| Proof-section flow field (landing/FlowFieldLazy.tsx on vendor/godui/flow-field) | Layout accents → godui | registry pull godui.design/r/flow-field | direct HIT | n/a | tokens ✓ (opaque bg → destination-out transparent trails; theme probing deleted; lo trails + 1-in-8 up; 260 particles area-clamped; IO ≥20% + visibilitychange; reduced → static frame) | pass-landing.xl ✓ |
| The-loop sticky reveal (how/sections/TheLoop.tsx on vendor/godui/sticky-scroll) | Choreographed scroll → godui | registry pull godui.design/r/sticky-scroll | direct HIT | n/a | tokens ✓ (full var map; original five-beat copy kept; sole motion owner — no GSAP conflict; axe fix this session: tabIndex+region+focus ring on the scroll panel) | pass-how.xl ✓ |
| Pipeline beams (how/sections/PipelineBeams.tsx on vendor/godui/animated-beam) | Layout accents → godui | registry pull godui.design/r/animated-beam (magicui's animated-beam not pulled — one owner) | direct HIT | n/a | tokens ✓ (gradient → hairline→hi travel; 4s calm; chain violet ONLY engine→Solana leg + node; reduced → static connectors) | pass-how.xl ✓ |
| Proof-flow graph (how/sections/ProofFlow.tsx on vendor/godui/agent-flow) | Finished surface → godui | registry pull godui.design/r/agent-flow (1041 lines, re-skinned throughout) | direct HIT | n/a | tokens ✓ (React Flow graph REPLACED — @xyflow/react off /how; nodes: fixture feed → merkle root → validateStat(chain) → settled; status up/lo never halt; readable without dragging; still lazy) | pass-how.xl ✓ |
| Settlement timeline (ProofFlow card on vendor/godui/agent-timeline) | Layout accents → godui | registry pull godui.design/r/agent-timeline | direct HIT | n/a | tokens ✓ (chain marker only on "Verified on-chain", mono sig + ProofBadge in the detail; m.spring; reduced instant) | pass-how.xl ✓ |
| Host-city map (how/sections/Coverage.tsx on vendor/magicui/dotted-map) | Layout accents → magicui | registry pull magicui.design/r/dotted-map (dep svg-dotted-map MIT) | direct HIT | n/a | tokens ✓ (static SVG, no canvas loop; 16 hardcoded host cities as up markers; SMIL pulse wrapped motion-reduce:hidden) | pass-how.xl ✓ |
| Win confetti (games/Verdict.tsx on vendor/magicui/confetti) | Animated candy → magicui | registry pull magicui.design/r/confetti (dep canvas-confetti ISC) | direct HIT | n/a | tokens ✓ (colors from --up/--text-hi via getComputedStyle; fires once per correct pick, ref-guarded; PRM → none; GAME-WIN joy only — zero payout/checkout language; ConfettiButton + dead button import deleted) | pass-play.xl ✓ |
| Round filter (games/RoundFilter.tsx + svar-ninety.css + roundLog.ts) | Gaming filter → SVAR | pnpm add @svar-ui/react-filter+react-core 2.6.0 (MIT) | context7 /svar-widgets/react-filter | n/a | tokens ✓ (every --wx-* remapped inside .games-filter; portal leak fixed via data-wx-portal-root + inverted scoping; fonts=false; types shim for broken published d.ts; localStorage round log via useSyncExternalStore; GAMING-ONLY) | pass-play.xl ✓ (empty state) |

## Component + dashboard pass (pass 2) — research & install ledger

Pulled via `apps/web/scripts/pull-registry-pass2.mjs`. Licenses verified per-source this pass:
| Source | Item | License (verified) | Note |
|---|---|---|---|
| registry.watermelon.sh (WatermelonCorp/watermellon-registry) | web3-dashboard block (15 files) | **MIT** (repo LICENSE) | Shell only — every web3/money affordance gutted per play-money law; recharts/@tabler/cva regDeps NOT installed (banned second chart lib / second icon family) — rewritten onto Sparkline/EquityCurve + lucide |
| dotmatrix.zzzzshawn.cloud (zzzzshawn/matrix) | dotm-square-3 (+core/hooks/css) | **Custom Proprietary** — app embedding (incl. commercial) expressly permitted; redistribution/resale as a component library prohibited | Flagged honestly: NOT open-source despite site copy. Our use (loading primitive inside the app) is within the grant |
| componentry.dev | dithered-logo (1 file) | **MIT** (site structured data) | Landing/splash accent only — never the 16px favicon |
| styleui.dev | notio (81-file template block) | **UNDECLARED** — JSON publicly served; site carries paid offerings | **LITERAL PULL (ADR-068)** — supersedes the earlier structure-study row. `shadcn add https://styleui.dev/r/notio.json` writes notio's real code; the landing (app/page.tsx) now ships notio's **navbar · hero · beam · features · feature-card · stats · counter · footer + ui primitives (button/card/badge/input/label/separator/toggle/dropdown/grid-pattern)**, all re-skinned to Ninety via a one-place shadcn-semantic token bridge (tailwind.config + tokens.css: bg-background→--bg, primary→--up, etc.). Ninety pieces injected: HeroRiver (hero right), Wordmark+WC26 (nav/footer), our copy + "Open the terminal" CTAs, skiper39 CrowdBand. **Dropped** (placeholder brand / play-money conflict / external CDN avatars): logo-cloud, team, testimonial, pricing, transcriber, tool/transcription-feature, theme-switcher chrome. Attribution: README Credits + landing footer. Deps kept: @base-ui/react, motion, clsx+tailwind-merge, tw-animate-css. |
| npm balloons-js@0.0.3 | balloon celebrations | **MIT** | On-brand swap: ⚽ + token-colored balloons on streak milestones; never the emoji defaults; PRM → never fires |

### Shipped rows — pass 2

| Component | Router row | Tool called | Searched (incl. misses) | Sofascore ref | Re-skinned | Shot |
|---|---|---|---|---|---|---|
| /account dashboard (features/account/* on vendor/watermelon/dashboard-shell) | Finished product surface → watermelon.sh block | registry pull registry.watermelon.sh web3-dashboard (MIT) — SHELL ONLY, guts replaced | direct HIT via repo tree (site 404'd the naive URL patterns — logged) | n/a (trader dashboard, Hyperliquid calm) | tokens ✓ (every money affordance deleted — asset tables, swap/deposit/withdraw, APY, wallet balance; recharts→EquityCurve, @tabler→lucide, shadcn regDeps→repo primitives; credits/positions/accuracy/moments/rank/proofs/watchlist; chain violet only via ProofBadge/mint chip; /portfolio → redirect) | p2-{landing,account,play}.xl ✓ |
| Loading primitive (components/ui/Loading.tsx on vendor/dotmatrix/dotm-square-3) | Animated candy → dotmatrix registry | registry pull dotmatrix.zzzzshawn.cloud/r/dotm-square-3 (custom-proprietary: embedding permitted) | direct HIT after WebSearch domain hunt (dotmatrix.dev/.style/.sh dead — logged) | n/a | tokens ✓ (currentColor active dots via text-up, idle --text-lo; css trimmed 26KB→4.2KB, hooks 5.6→2.7KB, core pre-trimmed 46→16KB; PRM → static grid + CSS freeze; role=status + sr-only label) | p2-{landing,account,play}.xl ✓ |
| Spinner replacements (godui/agent-timeline running-step, how/ProofFlowLazy) | Loading primitive rollout | composition over Loading/DotmSquare3 | full audit: animate-spin grep + route loader + Suspense fallbacks; board/terminal/depth have NO pending states (static WC26 + sync live merge — nothing to replace, logged); app/loading.tsx keeps PageSkeleton (content-shaped ≠ indeterminate) | n/a | tokens ✓ | p2-{landing,account,play}.xl ✓ |
| Milestone balloons (components/ui/MilestoneBalloons.tsx) | Celebration → balloons-js (npm) | pnpm add balloons-js@0.0.3 (MIT, Artur Bień); textBalloons() API — the emoji default (poop) NEVER called | n/a | n/a | tokens ✓ (⚽ line in resolved --up, "N in a row" in --text-hi; best-streak crossings 5/10 from roundLog; one-shot localStorage; PRM → never fires; /play only — off every live-price surface) | p2-{landing,account,play}.xl ✓ |
| Landing nav (landing/LandingNav.tsx) | Finished surface — notio STRUCTURE | hand-build to notio's sticky-nav slot (zero notio markup) | Four-registry search (design-cop gap 4): shadcn MISS (navigation-menu is an app dropdown primitive, wrong anatomy for a sticky marketing header) · magicui MISS (full 77-item registry listing holds no navbar/header item) · 21st.dev "sticky marketing navbar header logo links CTA" → top hits are CTA sections, no sticky-nav anatomy — MISS · OriginKit navbar category → 0 components, MISS. Hand-build legal; notio ref studied at design/reference/notio (license undeclared — structure only) | n/a | tokens ✓ (replaces the hero's inline header — duplicate wordmark/CTA would fail read-out-loud) | p2-{landing,account,play}.xl ✓ |
| Free-credits panel (landing/FreeCredits.tsx) | notio "pricing" slot, Ninety-inverted | hand-build (ONE card: Free / 1,000 credits every match / no deposits, no cash value, ever) | Four-registry search (design-cop gap 4): shadcn MISS (no pricing block in the base registry) · magicui MISS (77-item listing has no pricing item) · 21st.dev "pricing card single tier free plan" → HIT id 2172 "Single Pricing Card" — REJECTED: get_component returns PAID code + quota, and every line of a SaaS pricing card's content is discarded here (the section's point is the play-money inversion copy, not the box) — same reject class as the CompetitionsRail rows · OriginKit pricing category → 0 components, MISS | n/a | tokens ✓ | p2-{landing,account,play}.xl ✓ |
| Booth quotes (landing/BoothQuotes.tsx) | notio "testimonials" slot, Ninety-inverted | hand-build — the Booth's REAL lines from lib/terminal.ts (22'/68'/74', Ashour 31→55 consistent with moments.ts); no invented customers | notio testimonial structure only | n/a | tokens ✓ | p2-{landing,account,play}.xl ✓ |
| Dithered mark (landing/DitheredMark.tsx on vendor/componentry/dithered-logo) | Layout accents → componentry.dev | registry pull componentry.dev/r/dithered-logo (MIT) | direct HIT | n/a | CUT from the tree post-audit: the vendor dither runs an unconditional rAF tick and the live MotionScore measured MOBILE thrashing C→D with this as the only below-lg addition — gate rule applied (component survives at features/landing/DitheredMark.tsx; re-add needs an interaction-gated loop). Re-skin had been done (currentColor, no hex) | p2-{landing,account,play}.xl ✓ |
| notio template (81 files) | STRUCTURE STUDY ONLY | pulled to design/reference/notio — zero files ship; logo-cloud/team/theme-machinery skipped (no real logos, no team section, dark-only law) | styleui.dev/r/notio.json (public JSON, license undeclared — flagged) | n/a | n/a — nothing shipped | n/a |
| Ninety brand mark — River-9 (Logomark.tsx + Wordmark lockup + TerminalHeader + favicon.svg + og.png) | Ninety-specific / hand-build (brand iconography) | hand-build | shadcn/magicui/21st/logo_search n/a — a brand mark is bespoke by definition; law forbids template-brand decoration. Derived from the Momentum River (ADR-052/058), the product's own signature element | n/a (brand surface, not a football-depth lane) | tokens ✓ (head+stem currentColor, tail var(--up) the price-up token not decoration; mono prop folds tail to ink; favicon inlines brand hex on a dark tile per the standalone-SVG exemption). 3-direction grid rendered + judged at 16/24/32/48/96 on #0B0D10 before pick; design-taste + emil premium-not-template call PASS | live header ✓ (ninety-nu.vercel.app/board) · favicon 16px light+dark chrome ✓ · og 1200×630 ✓ (ADR-064) |

## Credibility fixes — /match/[id] parameterization + /proofs relabel (this pass)

No new third-party primitive was pulled: every row below is a hand-built **composition of existing Ninety pieces**,
so criterion 12 is satisfied by reuse, not by a fresh registry pull.

| Component | Router row | Tool called | Searched (incl. misses) | Sofascore ref | Re-skinned | Shot |
|---|---|---|---|---|---|---|
| Plain match column (terminal/PlainMatchColumn.tsx) | Ninety-specific — composition | hand-build (composes MatchHeader + MomentumRiver + PriceCells + TradePanel + YourPosition + MatchStates + SquadPitch) | registries n/a — the non-featured match terminal is the SAME Ninety spine as the featured column minus the AUS-EGY halt choreography; nothing to pull, everything reused | score-header.md + match spine (Polymarket outcome→prob→chart→trade) | tokens ✓ (BRA/home-win% river, derived LMSR from the store mark, no amber/violet) | match-live.{lg,xl}.png · match-pre.{lg,xl}.png ✓ |
| Plain depth tabs (terminal/PlainMatchTabs.tsx) | Generic primitive (tabs) → radix/shadcn | radix-ui Tabs (installed, same primitive as MatchTabs row) re-skinned; composes SquadPitch | radix Tabs is the sanctioned tabs primitive — reused, not re-rolled; honest subset (Lineups · Positions) since Stats/H2H/Events/Booth are AUS-EGY-only fixtures | tabs.md (depth-in-tabs) | tokens ✓ | match-live.lg.png · match-pre.lg.png ✓ |
| Squad-by-code pitch (terminal/depth/Lineups.tsx — SquadPitch export) | Football-product structure → sofascore-research — hand-build | hand-build (extends the existing Lineups faces pitch to address a match by team code, honest "pending" fallback) | reuses the ADR-062 baked TheSportsDB squad faces; API-Football XI still BLOCKED, so pending state stays honest | lineups.md | tokens ✓ (hairline markings, no team colours) | match-live.lg.png · match-pre.lg.png ✓ |
| ProofBadge honest-pending state (components/ui/ProofBadge.tsx) | Ninety-specific (one of the six) — hand-build | hand-build (real base58 sig → Solscan `<a>`; otherwise a non-link violet "proof pending" chip) | n/a — the ONE chain surface; settlement fail-closed (ADR-036/037) means no real sig, so it must never emit a dead Solscan href | n/a | tokens ✓ (violet on-chain only; Clock glyph for pending, ShieldCheck for verified) | proofs.{lg,xl}.png ✓ |

## Long landing — the 9-section page below the notio hero (ADR-069, this pass)

The long page is REUSE of already-built, MotionScore-tuned landing pieces (LoopStage, PriceVoid/PriceScrub,
VelocityBand, FlowField, WorldGlobe, BoothQuotes, FreeCredits, CrowdBand + the magicui HyperText/Highlighter/
NumberTicker pulls) recomposed under the kept notio hero, plus two new consumer sections. No fresh registry
pull was needed — the motion ceiling is met by the ~10 components already logged above.

| Component | Router row | Tool called | Searched (incl. misses) | Re-skinned | Shot |
|---|---|---|---|---|---|
| Football-experience journey (landing/FootballExperience.tsx) | Ninety-specific — compose a screen (ui-ux-pro-max) | hand-build (5-step fan journey: watch → River → Next Goal → Moment → Telegram) | registries n/a — this is Ninety product narrative (the Consumer-track pitch), no template has "the fan's journey through a football exchange"; steps are teasers (no live game mounts, keeps MotionScore clean) | tokens ✓ (lucide icons, up-accent on hover, data-arrive cascade) | long-landing.xl.png ✓ (LOOKED) |
| Consumer bento — Games/Moments/Telegram (landing/ConsumerBento.tsx) | Animated candy (bento, marquee) → magicui | compose magicui BentoGrid/BentoCard + Marquee (both already pulled + re-skinned, rows above) with Ninety content | reused the existing vendor/magicui/bento-grid + marquee — no new pull; Next Goal/Moments/Telegram are teasers linking to /play, /moments (the live game runs in the terminal, not the landing) | tokens ✓ (BentoCard hairline/elev; Booth-voice marquee chips; edge fades; no bet/stake/odds copy) | long-landing.xl.png ✓ (LOOKED) |
| Booth quotes → CAN–MAR (landing/BoothQuotes.tsx, supersedes row 120) | notio "testimonials" slot, Ninety-inverted | hand-build — re-homed from AUS–EGY (lib/terminal.ts) to inline CAN–MAR demo commentary | read-out-loud fix: the hero/loop/price are all the featured CAN–MAR market, so the Booth must be too — the AUS–EGY lines contradicted the page. CAN–MAR lines are illustrative demo narration for the same featured replay (not invented testimonials) | tokens ✓ (goal card up-accent, others quiet) | long-landing.xl.png ✓ (LOOKED) |
| LandingLong composition (landing/LandingLong.tsx) | Compose a screen (ui-ux-pro-max) + scroll spine (gsap-skills via LandingScroll) | hand-build composition | reuse: notio Navbar/Hero/Footer + the landing section components; scroll spine is the existing LandingScroll (GSAP ScrollTrigger, once-in-view, matchMedia + reduced-motion). Cut vs old LandingPage: skiper52 pillars (animated flex-grow = layout thrash) + the duplicate close | tokens ✓ | long-landing.{lg,xl}.png ✓ (LOOKED + halt WATCHED) |
| Scroll-scrub showpiece (landing/GoalReplayScroll.tsx + GoalReplayScrollLazy.tsx, ADR-078 pass 1) | BUILD scroll choreography → gsap-skills (per ui-craft router); technique from the `3d-scroll-website` skill (frame math / cover-fit / DPR / perf checklist) | hand-build: pinned full-bleed `<canvas>`, GSAP ScrollTrigger pin+scrub drives `frame = round(progress·N)`. context7-verified ScrollTrigger this session (pin · scrub:true · function `end +=360vh` · invalidateOnRefresh · refresh() after async decode) | registries n/a — a scroll-scrubbed frame-sequence is a bespoke technique, not a component to pull; **GSAP not Lenis** (CLAUDE.md law) — the skill's Lenis/raw-scroll reference code was NOT followed | tokens ✓ (var(--up)/--bg/--hairline/--text-lo + color-mix vignette; no raw hex; minor off-scale `text-[11px]`/inline `var(--text-section)` type nits) | scrub-{0..3}.png ✓ WATCHED (SEQ 4→34→64→93 advances, distinct frames, loader reveals, canvas 1440×900 guard PASS, 0 errors) |
| ⤷ frame asset for the scroll-scrub (public/frames/hero/, 96 JPG, 8.1M) | owner-supplied clip → ffmpeg `crop`(strip letterbox)+`fps`+`scale` slice | `Video-495.mp4` (owner-supplied), a portrait-container reel with the content letterbox-cropped (cropdetect `720:762:0:210`) to a near-square band; cover-fit in the landscape hero then crops the residual bars + the burned-in bottom subtitle out at the frame edges | **⚠ LEGAL — MECHANISM PLACEHOLDER, DO NOT SHIP PUBLICLY AS-IS:** an edited anime reel depicting a NAMED-PLAYER LIKENESS (Messi / #19, Argentina AFA + adidas marks) with burned-in subtitles + hard cuts between shots. A named player's likeness is exactly what CLAUDE.md's legal armor + the extractor's legal note forbid on the public landing — this does NOT resolve the pass-1 legal gate, it sharpens it. Swap for an ORIGINAL / LICENSED / ANONYMOUS (ideally single-shot) asset before any public deploy (owner aware). Mechanism is asset-agnostic. Supersedes the earlier /frames/cine iron-man placeholder (removed) | n/a (asset) | frames LOOKED (hero_0048/0060) + hero render LOOKED (hero-scrub-0..2: full-bleed, bars/subs cropped by cover-fit) |
| Crest wall (landing/CrestWall.tsx) — the football-warmth pass, SUPERSEDES the WorldGlobe row (:38) in this slot | Layout accents / generic grid → registries first | hand-build over the EXISTING TeamCrest primitive (crest→Flag fallback) + `data/wc26` teams.json | SEARCH RUN THIS SESSION, HITS FOUND AND REJECTED — logged honestly rather than claimed empty. **magicui** `searchRegistryItems "logo cloud grid of logos wall"` → MISS: ships only background grid *patterns* (grid-pattern / animated-grid-pattern / flickering-grid) + bento-grid; no logo wall. **shadcn** `search_items_in_registries` → n/a, no registries configured in components.json. **21st** `search "logo cloud wall grid of brand logos"` → 4 real HITS (educlopez/logo-cloud, sshahaider/logo-cloud-2·3·4, smammar100/pixel-logo-grid). REJECTED, reason: every one models flat "trusted by" social proof (a 5-col brand row with decorative plus-icons / canvas pixel-shimmer hover). This wall is not social proof — it is the WC26 DRAW: 12 groups × 4, and the grouping is the entire point (a fan navigates by group). Re-skinning a logo-cloud to 12 grouped clusters + stripping its shimmer to tokens = rewriting it; the pull would also need a paid API key. Ninety data, not brand logos | tokens ✓ (no hex — floodlight is `color-mix(in srgb, var(--up) 26%, transparent)`; crests rest `md:opacity-80` so the `text-stat` numbers stay loudest — football subordinate to data) | landing-after.{lg,xl}.png + landing-crestwall.png ✓ (LOOKED; 48/48 imgs render, 0 broken) |

## The 3D football moment — DribbleScene (ADR-070, this pass)

| Component | Router row | Tool called | Searched (incl. misses) | Re-skinned | Shot |
|---|---|---|---|---|---|
| Dribble scene (landing/DribbleScene.tsx + DribbleSceneLazy.tsx) | 3D/shader → landing-only (ADR-058), hand-build | hand-build on npm `three` (already in deps; vanilla-three per the house WorldGlobe pattern — NOT R3F Canvas, NOT drei: drei is not installed and the scene needs no loader). context7-verified this session: CatmullRomCurve3 getPointAt/getTangentAt (+optionalTarget/lookAt no-alloc pattern), CapsuleGeometry(radius,height,capSegments,radialSegments) | Registries n/a — no registry ships "a stylized footballer weaves and scores"; this is Ninety-specific narrative 3D. REALISTIC PATH BLOCKED HONESTLY: Mixamo rigged clips (dribble/kick) require an interactive Adobe login — headless download impossible; no clearly-licensed redistributable football GLTF found. The prompt's sanctioned STYLIZED FALLBACK is the ship, and it measured cleaner (steady-state 0 long tasks) | tokens ✓ via resolveColor (bg pitch, up lines/kit/glow/trail/flash, hairline defenders, textHi frame/ball, textLo net) — law-guard blocked the one hex fallback during authoring and it was removed; chip is DOM (.num, .flash-up) | scene-frame-0..5 WATCHED (run→beaten defenders→goal+61.4→reset); verdict 2026-07-17-dribble-scene.md |

## Entity search (ADR-081, Fix-2 — Session B)

Sofascore-class ⌘K entity search. The index is BAKED once and searched locally — zero third-party calls per
keystroke (the provider budgets, 10/min and 100/day, make search-as-you-type-over-API impossible). Roster
data is STILL data (ADR-051), so baking it is correct; TxLINE still owns everything that moves. Verification
is the single merged pass on the main tree (PARALLEL-PLAN.md), not a per-session server.

| Component | Router row | Tool called | Searched (incl. misses) | Re-skinned | Shot |
|---|---|---|---|---|---|
| Roster bake (scripts/fetch-squads.mjs → data/wc26/players·coaches·referees.json) | Baked data pass (extends wc26:refresh) — not a UI component | hand-build; football-data.org v4 competition 2000 (the real WC2026). PROBE-FIRST per the honesty gate: `/competitions/2000/teams` **embeds squad+coach**, so the whole roster is ONE call (not the 48 the plan budgeted) + one `/matches` for referees. 2 calls total | provider was the spec (football-data.org); reused the already-baked TheSportsDB `media.json` for player photos by slug — football-data has none. Real counts: players 1249 (404 photo-matched) · coaches 46/48 · referees 42. Refuses to write on any unmatched team (no silent gaps) | n/a (data) | counts logged in ADR-081; JSON spot-checked (all 48 teams covered) |
| entitySearch.ts (lib/data/) | Compose a screen (ui-ux-pro-max) — client search index | hand-build; ranking = exact>prefix>word-start>substring, tie-broken by importance (teams>players), per ADR-081 | n/a — bespoke over the baked JSON; dynamic-imported by CommandMenu so the 285 KB player index never enters the initial bundle | n/a (logic) | tsc ✓ |
| CommandMenu.tsx (components/ui/) — the palette rebuild | Compose a screen (ui-ux-pro-max via ui-craft router) | re-skin the EXISTING `cmdk` base (kept for combobox/listbox a11y + keyboard nav); category pills, Recent (localStorage) + Suggested, two-line rich rows, skeleton/empty states, ⌘1..9 hand-wired | reused cmdk + the house TeamCrest/Flag primitives; dropped Traders (no verified /profile page → honesty gate) + kept Pages nav. Honesty gate: Match/Team/Competition navigable (real pages), Player/Manager/Venue informational (no href → cannot 404) | tokens ✓ (active pill `bg-up text-bg`; Clear-history in `--chain`; mono+tabular ONLY on minute/kickoff; motion-safe pulse; no raw hex, no bet/stake/odds copy) | verified in the merged pass (PARALLEL-PLAN §Verification) |
| apple-spotlight (21st.dev) — motion shell over the palette (amendment 2026-07-18) | Add/integrate a component (ui-ux-pro-max via ui-craft router) | 21st.dev pull; **motion vocabulary only** via `framer-motion` ^12 — zero installs | TOOK: spring open/close (scale + un-blur, morph-not-pop), spotlight placeholder echo (hovered row's name, `data-echo` delegation so cmdk registration is intact), chevron-on-hover. DROPPED: demo data, ShortcutButton dock (we have pills), `target=_blank` (honesty gate → internal `router.push`), one-line rows (kept two-line + 40px avatar). Shell: `createPortal` + `AnimatePresence` around raw `Command` (cmdk Dialog can't exit-animate) | tokens ✓ (no raw hex/palette; play-money copy). Gooey `url(#ninety-goo)` + animated blur **gated OFF `/terminal`·`/board`·`/match` + reduced-motion, transition-only** (ADR-058 tick path). a11y re-added on the shell: role=dialog · aria-modal · esc · focus trap/restore · scroll-lock | build ✓; dark+light+3 states + tick check in the merged pass |

## Player pages (ADR-082, Fix-3 — Session C)

Sofascore-class `/player/[id]` for the top-20 WC26 scorers, so search rows and lineups get a real destination.
Built ONLY on what the data tier honestly supports (ADR-051 STILL data): identity + FINAL results from
football-data, our own market data for the differentiator. The panels the reference shows but we can't source
(market value, followers, official rating radar, heatmaps) are ABSENT, not faked (see ADR-082). Verification is
the single merged pass on the main tree (PARALLEL-PLAN.md), not a per-session server.

| Component | Router row | Tool called | Built (incl. what's omitted) | Re-skinned | Shot |
|---|---|---|---|---|---|
| bake-player-profiles.mjs (scripts/ → data/wc26/player-profiles.json) | Baked data pass — not a UI component | hand-build; football-data.org v4 comp 2000. PROBE-FIRST: API-Football free plan is season-blocked for 2026 (`"try from 2022 to 2024"`) → 0 API-Football data calls. Bake = 1 `/scorers` + 20 `/persons/{id}/matches` = 21 calls, throttled ~8/min, resumable cache | derived the top-20 by goals+assists (reproducible); real WC26 match logs (date·stage·teams·score→W/L/D); Ninety index derived from per-match production. OMITS ratings/minutes/season-aggregates (plan-blocked), height/foot/club (no tier source) — never invented | n/a (data) | 20 players · 113 matches · read-out-loud ✓ (ADR-082) |
| features/player/* (PlayerHeader · PlayerPhoto · MatchesTable · SeasonPanel · NinetyIndex · MarketImpact · PlayerTabs · PlayerRail · PlayerPage) | Compose a screen (ui-ux-pro-max via ui-craft router) | hand-build over the baked JSON; reused house primitives (TeamCrest, EquityCurve, fmtPrice/signedPct) + Next `next/image` for baked photos; SVG radar hand-drawn (no dep) | photos: baked-local (7/20) else initials avatar + nation badge (no CDN/redistribution, ADR-055). Only tabs WITH data (Matches · Season) — no empty Fantasy/Media. Market impact = TEAM-level modeled moments (never per-goal fabrication) + honest empty state. PlayerRail = cross-nav across the 20 (no dead-ends) | tokens ✓ (`--up`/`--down`/`--hairline`/`--chain` off tokens; mono+tabular ONLY on numbers/prices; radar in `--up`; focus rings; `prefers-reduced-motion` safe; no raw hex; play-money copy) | prod build SSG ✓ (676 B / 112 kB); read-out-loud on Messi/Vinicius/Haaland ✓ |
| players.ts (apps/api/http/routes/) — read-model + `lib/api` clients + `features/player/loaders.ts` | Compose a screen (ui-ux-pro-max via ui-craft router) — CONNECT read-model | hand-build Fastify; re-serves the baked file for non-web consumers (search, lineups); never calls a provider; 503 {needs} when unbaked (same honest-degrade as rich-data proxy) | `GET /players` (top-20 index) + `/players/:id` (full profile, 404 unknown). Consumed the CONNECT way: `lib/api` typed clients → loaders PINNED to baked fixtures (SSG-friendly), live paths kept as un-pin target (mirrors `lib/data/moments.ts`); page + PlayerRail render through the loaders (clears the no-dead-code gate) | n/a (API) | `players.test.ts` ✓ (20 rows · id resolve · 404 · 503); gate ✓ | 

## Team pages (ADR-083, Session D)

Sofascore-class `/team/[code]` for the 48 WC26 nations + 10 clubs, so every board/search/match link resolves to a
real team page. Built ONLY on what the free tier honestly supports (ADR-051 STILL data) + our own market data for the
differentiator; the panels a stats site shows but we can't source (followers, market value, media, per-match ratings)
are ABSENT, not faked. Verified on a local prod build (verdict: `design/verdicts/team-pages-adr083.md`).

| Component | Router row | Tool called | Built (incl. what's omitted) | Re-skinned | Shot |
|---|---|---|---|---|---|
| bake-team-profiles.mjs (scripts/ → data/wc26/team-profiles.json + public/crests/*) | Baked data pass — not a UI component | hand-build; football-data.org v4. Competition-level, not per-team: 3 calls cover all 48 nations (`/competitions/2000/{teams,matches,standings}`); clubs = 2 each + 5 deduped league standings ≈ **28 calls**, throttled 6.5s, resumable cache | joined squads+coaches from the already-baked players.json/coaches.json (never refetched); nations reuse baked flags, clubs bake crests locally (ADR-055); **strips every CDN url** (match/standings crests, comp emblems) — 0 CDN asset urls in the JSON. Real: 48 nations + 10 clubs | n/a (data) | counts logged; read-out-loud ✓ (ADR-083) |
| features/team/* (TeamHeader · MatchCards · MatchesPanel · RecentForm · MarketView · StandingsTable · StatisticsTab · PlayersTab · DetailsTab · TeamTabs · TeamRail · TeamPage · Crest) | Compose a screen (ui-ux-pro-max via ui-craft router) | hand-build over the baked JSON; reused house primitives (TeamCrest/Flag, EquityCurve, fmtPrice/signedPct, next/image) + the proven features/player pattern; data.ts (pure, client-safe) vs loaders.ts (server-only) split keeps the 836 KB JSON out of client bundles | crests: baked-local only (no CDN, ADR-055); only tabs WITH data (Standings guarded to games-played, no empty Media); MarketView = TEAM-level modeled moments + market-vs-reality join (never per-goal fabrication) + honest empty state; squad links only for baked profiles (no 404s); TeamRail = group-mates/league rivals (no dead-ends) | tokens ✓ (`--up`/`--down`/`--hairline`/`--chain` off tokens; mono+tabular ONLY on numbers; qual-zones = --up/--down left border; focus rings; play-money copy "price/trade/credits") | prod build SSG ✓ (2.94 kB / 132 kB); read-out-loud on ESP/CAN/Barcelona/Man City ✓; 4 fixes re-shot |
| teams.ts (apps/api/http/routes/) — rich-data proxy | Compose a screen (ui-ux-pro-max via ui-craft router) — CONNECT read-model | hand-build Fastify; `GET /rich/teams/:id/matches` over the cost-aware cached proxy (the one endpoint richdata.ts didn't cover); STILL data only; 503/429/502 honest-degrade | registered in server.ts alongside the other /rich/* routes; the page is pure SSG from the baked JSON and never calls it at runtime (it's the CONNECT un-pin target) | n/a (API) | tsc/build ✓ |

## Landing finale (ADR-085) — layered crest-parallax closer

The landing's closing statement: a layered football parallax closing on a huge NINETY wordmark. Technique from
21st.dev `mountain-vista-bg` (background planes at different speeds/sizes/z-indexes); everything else re-built.
Landing-only (ADR-058), never a live-price surface.

| Component | Router row | Tool / source | Built (incl. what changed from source) | Re-skinned | Shot |
|---|---|---|---|---|---|
| LandingFinale.tsx + LandingFinaleLazy.tsx (features/landing/) | Landing / anti-slop taste (design-taste-frontend via ui-craft router) | technique from 21st.dev `mountain-vista-bg`; hand-built with framer-motion already installed (zero installs) | SUBJECT swapped mountains+cyclists → football (our 48 crests, `public/teams/{id}/badge.png`). CDN removed (source hotlinked cdpn.io/amazonaws → all local, ADR-055). Motion = `transform: translate3d` on duplicated tiles (`.pfin-track` in globals.css), NOT `background-position` (GPU-composited, no per-frame repaint). Plan's `stadium.jpg` plane DROPPED — those files are fan/squad/flag photos + a third-party France-WC2018 poster (borrowed art, STEP 0b) → replaced with a second dim/slow crest plane | tokens only (bg-token scrims tint the scene per theme so NINETY's text-hi reads in dark AND light; floodlight in `--up`; pitch in `--up`/`--hairline`; no raw hex; play-money copy; reuses the locked `CtaPair` funnel — no new CTA intent) | prod build ✓; looked lg+xl both themes; drift frames differ (motion), reduced-motion frames byte-identical (static); zero cdpn.io/amazonaws in built output |

## Component swap + integration pass (2026-07-19) — SHRINK ledger

**Measure of success = codebase SMALLER.** Baseline `apps/web/src` = **26,082** lines → after = **25,426** lines
= **−656 (−2.5%)**. Clean prod build green; protected list untouched; zero references to anything deleted.

**Governing decision (why so few "swaps"):** the swap-table candidates are already *minimal, token-clean* wrappers
(ScrollArea 22 · Tooltip 25 · HoverCard 22 · Toaster 29 · Skeleton 47 · Loading 33 · Sparkline 29 · EquityCurve 33
· RailCard 23 · NumberTicker 48 · Reveal 27 lines). Replacing a 22-line token-native primitive with the
Radix/shadcn/recharts equivalent *grows* the code and *adds a dependency* — which this task's own rule defines as
failure ("if it grows, you layered"). So the path to "smaller" was **deleting dead + low-value code**, not
swapping tiny-for-bigger. Every kept primitive was grepped token-clean (zero raw hex / gray- / neutral-).

### Deleted (the shrink) — component · was · why
| Removed | Lines | Why |
|---|---|---|
| `components/ui/dropdown-menu.tsx` | 254 | Unused shadcn primitive (0 import sites) — pulled, never wired. Dead code. |
| `components/vendor/magicui/video-text.tsx` | 134 | Dead vendored pull (0 sites); not in the placement map. |
| `components/ui/toggle-group.tsx` | 89 | Unused shadcn primitive (0 sites). Dead code. |
| `components/ui/MilestoneBalloons.tsx` | 73 | "Earns little" (swap table) — /play-only balloon; only `balloons-js` user → **dep dropped** too. |
| `components/ui/grid-pattern.tsx` | 70 | Unused (0 sites) AND raw `gray-`/`dark:*-gray` classes (token violation). |
| `components/ui/StubScreen.tsx` | 34 | Unused (0 sites). Dead code. |
| dep `balloons-js` | — | Sole user (MilestoneBalloons) deleted. |

### Swap-table audit — KEPT (justified: swapping fails the "smaller" measure)
| Candidate | Verdict | Justification |
|---|---|---|
| Avatar | **KEEP** | Already the deterministic initials-in-a-token-disc identicon the swap asked for — **no pravatar, no CDN, no fake faces** (the bug is already fixed). shadcn avatar is a Radix image+fallback → would *reintroduce* the CDN pattern and grow the code. |
| ScrollArea, Tooltip, HoverCard, Toaster, Skeleton, Loading | **KEEP** | 22–47-line token-clean wrappers. Each shadcn/Radix equivalent is larger + adds `@radix-ui/*` — net growth = the failure condition. |
| Sparkline, EquityCurve | **KEEP** | 29/33-line dependency-free token SVG charts. recharts is hundreds of KB + a bundle risk on the very surfaces (terminal/moments) that must stay light — swapping is a large *growth*, not a shrink. |
| NumberTicker, RailCard | **KEEP** | Token-clean, no smaller vendored equivalent (magicui has no number-ticker vendored; RailCard is already a token card). |
| Reveal | **KEEP** | framer, but hydration-safe (SSR plain-div), reduced-motion-honouring, token-native, below-fold. No library reveal is smaller/better; swapping grows the code. |
| lowercase `button/card/input/label/badge/separator/toggle` | **KEEP** | Verified shadcn-shaped + token-clean (the two unused ones — dropdown-menu, toggle-group — were the dead ones, now deleted). |
| CommandMenu (523) | **KEEP the IA** | The rebuilt Sofascore-class ⌘K palette (ADR-081). The apple-spotlight *motion shell* is an additive enhancement (would grow) — deferred to its own amendment, not a replacement. |
| features Ticker / marquee / dock / Hero / HeroGradientField | **KEEP** | Recently hand-built to spec (Ticker = the broadcast strap, ADR-080) or already mapped to vendored magicui (marquee, scroll-based-velocity, dock). No *generic duplicate* to delete was found. |

### Prompt-B 30-effect integration — DROPPED as a bulk pass (a PASS per the governing rule)
The governing rule is explicit: "a component earns its place or it does not ship… drop it. That is a pass, not a
failure." A 30-effect checklist is dropped for the following reasons, not layered in:
1. **Primary measure is SMALLER.** Pulling 30 landing/marketing effects grows the codebase — the opposite of the win
   condition this pass is graded on.
2. **Already vendored** — `dithered-logo`, `marquee`, `scroll-based-velocity`, `dock`, `bento-grid`, `dotted-map`,
   `confetti`, `hyper-text`, `magic-card` already exist in `components/vendor/*`; re-pulling would duplicate (0b).
3. **Live-price GPU ban** — electric-border/liquid-distortion/pixel-canvas/fluid on /terminal · /board · /match
   contend with the 150ms tick path (ADR-058); most of the placement map targets live surfaces indirectly.
4. **anime.js prerequisite deferred** — animejs is NOT installed and `lib/anime.ts` doesn't exist. Installing a dep +
   wrapper + editing law-guard for effects I'm not shipping this pass is speculative (YAGNI). Do it the day the first
   anime-based effect actually ships.
5. **Right cadence** — each effect should land one-at-a-time with its own screenshot and long-task check, exactly like
   the swap process; a bulk import is the slop both prompts warn against. Left as a backlog, each to earn its place.

## Landing static hero (ADR-078 pass 2, 2026-07-19)
| Component | Source | Licence | Placement |
|---|---|---|---|
| `public/hero-stadium.svg` (the hero backdrop) | **Generated** — hand-authored token-palette SVG (ours); floodlight banks + light beams + a darkened green pitch. No photograph, no person, no club/national kit, no brand mark, no external asset, no AI-image service. | **n/a — original generated asset** (nothing to license, nothing to attribute; not a press photo, not fan art, no watermark). | Landing BEAT-1 hero (`features/landing/StaticHero.tsx`), full-bleed 16:9, CSS `cover` background + `--bg` legibility scrim. Replaced the retired `/frames/hero` scroll-scrub (a named-player-likeness placeholder, deleted). |
