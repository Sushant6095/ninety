import { CompetitionPage } from "../../features/competition/CompetitionPage";

// /competition — WC26 group standings (static context, baked from worldcup26 · ADR-051).
export default function Page() {
  return <CompetitionPage />;
}
