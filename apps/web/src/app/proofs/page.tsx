import { StubScreen } from "../../components/ui/StubScreen";

// Proofs — the on-chain settlement proof log (TxLINE → Solana devnet). Route is live so the Terminal sub-nav lands.
export default function Page() {
  return <StubScreen name="Proofs" note="The settlement proof log (TxLINE consensus → Solana devnet) lands here. The route is live." />;
}
