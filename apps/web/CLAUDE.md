# web/ session memory
Design law lives in root CLAUDE.md; tokens in src/styles/tokens.css; references in design/screens/.
Build primitives before pages. Every screen ends with a design-cop subagent review against its PNG. Numbers are mono, prices one decimal, no gambling vocabulary in copy (hook-enforced).
