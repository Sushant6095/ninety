// uWS gateway. Channels: m:{match}:prices|events|booth, lb:global.
// Per-channel monotonic seq; resume via Redis stream backfill (see web/src/lib/ws.ts).
export async function startWs() { /* extraction-ready: no imports from engine/ or http/ */ }
