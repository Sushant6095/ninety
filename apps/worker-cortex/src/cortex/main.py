# bus in: odds.raw, match.events → bus out: prices.marks {fair, hazard, b_hint}
# fair = 0.2*model + 0.8*devigged_consensus. Consensus is king; model supplies hazard + sanity.
def main():
    pass  # subscribe → update → publish loop

if __name__ == "__main__":
    main()
