# cortex session memory
Formulas are canon: Shin de-vig → Dixon-Coles + particle filter → fair = 0.2·model + 0.8·consensus → hazard drives b(t). Use penaltyblog, don't reimplement. Changes here get a quant-reviewer subagent pass.
