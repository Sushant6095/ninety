node_modules/
.next/
dist/
*.tsbuildinfo
.turbo/
.env*
!.env.example
# Solana signing keypairs (SETTLEMENT_KEYPAIR_PATH=./.keys/…) — NEVER publish to a public repo
.keys/
*.pem
**/id.json
Video-*.mp4
*.log
__pycache__/
.venv/
target/
.anchor/
test-ledger/
.DS_Store
.playwright-mcp/
# appended by AI-native layer
.claude/trace/*.jsonl
.claude/trace/*.log
.claude/settings.local.json
# internal playbook — keep PRIVATE, never ship (the prompts + session context are the build recipe)
.claude/prompts/
.claude/context/
# ui-craft loop — generated screenshots (keep the dir, ignore the shots)
design/screens/impl/*
!design/screens/impl/.gitkeep

# agent trace logs (session noise — never ship)
**/.claude/trace/

# vendored skill packs (installed via `npx skills add …`) — keep LOCAL, never ship ~18 skill dirs
.agents/skills/

# stray root artifacts (screenshots + a duplicate skill-pack download) — never commit
/ours-*.png
/3d-scroll-website-skill-pack*
