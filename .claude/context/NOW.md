# NOW — the only mutable context file (injected into every session by SessionStart)
GOAL: ship the OMNIPITCH vertical slice by Jul 19 (demo recorded during SF Jul 14–15)
DONE: platform bring-up green (build/lint/test · Postgres 5433 + Redis · prisma init · dev boots web:3000/api:4000 /health · law-guard hook) · schema event contract (AnyEvent union) · bus: RedisBus over Streams + driver-agnostic contract suite (order/at-least-once/ack), KafkaBus stub conforms, BUS_DRIVER flip documented. See ADR-007/009/010/011/012; ADRs are immutable-by-policy (adr-scribe rule 5).
DONE(txline) 05 GREEN (world-verified): LIVE devnet subscribe→activate→snapshot printed a real WC fixture (USA v Belgium); IDL-built subscribe (chain), 8 typed wrappers with schemas reshaped to REAL payloads, real samples in docs/txline-samples. Reality wins: devnet=SL1 (SL12=mainnet), F1=/api/fixtures/snapshot, activate→bare-string token, goals from Score.Total.Goals. Mock CI = pnpm test; live = chain/scripts/txline-live.mjs. See ADR-013/014/015 · VERIFY states in .claude/prompts/PROGRAM-STANDARD.md. ⚠ Day-0 left: Stats-map key table.
DONE(ingest): worker-ingest SSE→normalizer→bus — odds.raw (loose Envelope; cortex de-vigs → marks) + match.events (goals by diffing); (source,source_seq) dedup (odds seq=hash(MessageId), scores seq=baseSeq*8+team+idx — globally unique, fixes false-drop collision); partition=match_id; fixtures→compacted Redis hash. Gap recovery (ADR-019): per-stream heartbeat watchdog (txline onHeartbeat) → >20s silence/drop during LIVE fetches snapshot, re-emits canonical events recovered:true (deduped, per active fixture), publishes feed.gap.v1, reconnects backoff+jitter. Exactly-once hardened after adversarial review (6 fixes): monotonicity guard (no stale-snapshot dup), journal-then-ack (no publish-fail goal loss), one-event-per-goal (multi-goal gap), generator close on catch. VERIFY: 10 worker-ingest tests green (kill mid-stream → recovery, no missed/dup goal; XLEN grows +unique, dup dropped). live needs chain subscriber. See ADR-018/019.
DONE(sys-plane+replay): two bus planes (ADR-020) — SysEvent (sys.signals.v1: feed_gap/backpressure/saga_stuck/replay_request) rides packages/bus beside AnyEvent; Bus topic-bound (PayloadOf<T> → a wrong-plane handler won't compile); feed.gap moved OFF its raw redis stream onto the bus; CLAUDE.md law amended = two planes, raw Redis/Kafka only inside bus (except engine journal + WS resume). Fixture replay (ADR-021): POST /admin/replay {match_id,speed} (admin token, timing-safe) → replay_request signal → worker main.ts walks a finished fixture's buckets, re-timestamps relative, publishes SAME topics at Nx; source=replay so live/replay are distinguishable; per-match_id isolation + concurrency cap; scripts/replay.sh wired. FOLLOW-UP(engine): must gate source=replay for a live match + finished-fixture allowlist before wiring settlement. VERIFY green (build/test/lint 9/9): feed.gap consumed via bus; goal at correct relative minute; concurrent replays isolated. See ADR-020/021.
DONE(cortex/pricing) — PART-1 DATA SPINE COMPLETE: worker-cortex (Python) consumes odds.raw → Shin de-vig (penaltyblog) w/ POWER fallback (TxLINE ships DEMARGINED books, booksum≤1 → Shin root-finder raises → live path is power) → fair sums to 1, keyed by the market's REAL outcomes (over/under·part1/part2, NOT 1X2 — feed reality ADR-015) → prices.marks. fair=0.2·model+0.8·consensus (model 0 in v1; Dixon-Coles+particle filter deferred). Hazard v1 = tick-variance + goal-spike(exp decay τ90s) + late-game scaling; b_hint=b0(1+κ·hazard) clamped [b0,3b0]. bus.py = the Python arm of packages/bus (same XADD {key,data} wire format). Schema: Mark.fair→record(string,number), EventSource+=cortex. 20ms/tick. quant-reviewer 6/6 PASS. VERIFY green: marks sum 1±1e-6, Shin matches hand-computed, goal→hazard spike/decay. FOLLOW-UP: Dixon-Coles model term; CLAUDE.md line for the Python bus arm. See ADR-022.
DONE(surfaces/52B EarlyWhistle) — deps-gate OVERRIDE (52 did not exist; built 52+52B fused): Telegram live match cards in apps/worker-jobs. Pure renderCard(state) shared by the pinned channel card + /match one-shot; MarkdownV2 (monospace ``` price table, escape everything incl. the +sign, ≤3500 via input-cap not slice); booth-voice copy filter. Per-market ring buffer (15min/512) → 16-bucket sparkline (frozen on halt). Global send Scheduler: one 25/s bucket, events>edits, 4s/msg throttle, coalesced edits, 429 backoff, events never dropped, no pump re-entrancy. State machine per match_id (independent): kickoff post+pin → 8s cadence / immediate on goal·red·halt·settled → HALTED header+freeze → SETTLED final edit + Solscan proof + unpin + Moment photo. Fire-and-forget, Δms log, settled-card eviction, injectable TelegramClient (no dep). Adversarial 4-lens review → 9 defects fixed (1 critical MdV2 '+' that would 400 every card). VERIFY green: 25 tests (golden card, scheduler, 2-match isolation, 429). FOLLOW-UP: Redis-backed fixture/leaderboard/moment readers + getUpdates loop in main.ts (stubbed). See ADR-023; ledger row 52B.
DONE(engine/market): market lifecycle machine (apps/api/src/engine/market.ts) — PURE reducer SCHEDULED→OPEN→LIVE⇄HALTED→RESOLVING→SETTLED|VOIDED, time ONLY from event payloads (never wall clock). Explicit 7×7 guard table: legal (emits 1 effect) · tolerated no-op (redelivery/late feed) · illegal→typed IllegalTransitionError. Triggers open·kickoff·pivot(goal/red/pen)·reopen·ft·settle·abandon; effects open/live/halt(3×)/halt_extend/reopen(decay 3→1 60s)/resolving/settled/voided. ADR-005: red-during-halt EXTENDS not re-halts; reopen policy from event-time. VOID refunds EXACTLY committed credits; create-or-park registry (unknown market never crashes); canAcceptOrder OPEN/LIVE only (MARKET_HALTED). engine-guardian: 6/6 invariants PASS + found 3 issues fixed (settle rejects missing result/sig — no placeholder settlement; haltedAt/reopenAt use clamped event-time; fromEnvelope drops bad ts). VERIFY: 65 tests incl. every illegal edge + all 6 enumerated edges. FOLLOW-UP: RESOLVING-abandon vs on-chain-settled interlock. See ADR-024.
DONE(engine/runtime): durable journal + per-market serialized loop (apps/api/src/engine/{journal,index}.ts). Journal = append-then-ack (durable BEFORE apply → crash-after-append replayed EXACTLY once); per-key monotone n; snapshot every 10k; recover = snapshot + contiguous-tail replay; harderrors never-silent (n-gap = trimmed-below-snapshot, hash-mismatch = torn snapshot); deterministic hashState; storage injected (Mem for tests, Redis prod = the named raw-Redis exception, injected client / type-only import). Engine loop = per-market promise-chain lane (strict arrival order across marks+events), pipeline validate→journal→apply→emit, commands-during-recovery queue, queue-cap→typed BACKPRESSURE, single-writer lease (SET NX PX → 2nd process exits loud), AMM tether (marks re-anchor b/spread; amm stays pure). engine-guardian 6/6 PASS + fixed CRITICAL (recover fail-safe quarantine, no fresh-seed wrong state) + HIGH×2 (bus adapter awaits durable + redelivers on backpressure = end-to-end journal-then-ack). VERIFY: 14 tests — 10.5k-cmd replay==live hash, crash recovery, exactly-once, torn/gap/quarantine edges; 2×1k per-market ordering; lease/backpressure. FOLLOW-UP: effect outbox (at-least-once emit), lease fencing token, tail-truncation watermark, per-market consumer partitioning. See ADR-025.
DONE(engine/order): order EngineCmd (ADR-026) — pure applyOrder(): risk gate (INVALID_SIZE·INVALID_OUTCOME·MARKET_HALTED·RATE_LIMIT ≤5·s⁻¹·SLIPPAGE·INSUFFICIENT_BALANCE@1e-9-eps·POSITION_CAP≤5000·INSUFFICIENT_POSITION) → LMSR fill @ live b (=bHint/(hazard-spread × ADR-005 reopen-decay via spreadAt, floored)) → 1% BANKER'S-rounded fee BURNED → q + position deltas + typed credit-ledger effects (debit/credit/burn); sell symmetric; server price wins, client reconciles via optional limit. amm math stays in amm.ts. engine-guardian 6/6 + FIXED CRITICAL (unvalidated outcome NaN-bricked the market / free fill) + HIGH (reopen decay now applied to fill b) + LOW cleanups. VERIFY green: cost vs independent closed-form <1e-9 (503 cases); fee-burn round-trip shrinks supply by EXACTLY the two burns; every reject unit-tested (incl RATE_LIMIT 5th-fills/6th-rejects + 1e-9 balance boundary); replay reproduces q/positions from stream AND snapshot. Gate 9/9, 102 api tests. FOLLOW-UP: ledger = ATOMIC balance authority + compensation path (engine balance gate is ADVISORY across markets); effect outbox now carries money (ADR-025 H3); lock burn≠debit ledger contract; order SOURCE gateway (http/ws feeds OrderCmd w/ authoritative balance + rate window). See ADR-026.
ACTIVE: 07 VERDICT = Plan A (CPI txoracle.validate_stat) — proven live on devnet (sig 4JRA6q…, 282,851 CU « 1.4M max; ADR-017). proof.rs plan updated; proof-auditor C1–C4/H2 baked into the wiring plan. NEXT: 07 wiring (bind market · pin roots PDA · gate FINISHED · derive predicate in-program · strict get_return_data) + STEP 0 goals-key/FINISHED — needs anchor toolchain unblock.
NEXT 3: 1) order SOURCE gateway (http/ws) + ledger consumer (ATOMIC debit + compensation) + effect outbox (money path, ADR-026/025 follow-ups)  2) 07 wiring settle_market CPI (after toolchain)  3) north-star screen via /screen match-live
BLOCKED: anchor build/deploy — platform-tools cargo 1.79 can't parse edition2024 transitive deps (block-buffer/zeroize); needs newer platform-tools (cargo ≥1.85) or pinned Cargo.lock (ADR-017). Does NOT block txline-env or 06.
(Keep this ≤10 lines. Update it when focus changes; /trace nags if stale.)
